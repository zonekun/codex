# TDNET 2024年 歯抜けデータ バックフィル計画

**作成日**: 2026-04-25
**基準 commit**: 822e8a0
**ステータス**: ✅ 全完了（2026-04-26確認）/ Run1 H1 完了（04/25 12:15→15:49, 3h34m, +16,498 docs）/ Run2 H2 完了（Q3: 04/26 07:44→09:42 7,178docs, Q4: 04/25 誤投入→完走 8,669docs）

---

## 問題の概要

2024年の旧アーキ バックフィル（#1-#23、30,303件 insert）で、`phase5_bq_insert` のフィルタバグ `[d for d in docs if d.text and d.chunks]` により **Embedding対象3カテゴリ（決算短信/決算説明資料/月次開示）以外が全ドロップ**された。

| 項目 | 件数 |
|------|------|
| GCS 2024 PDF | 66,362 |
| BQ 2024 DOC_ID | 33,998 |
| **欠損** | **32,395 (48.8%)** |

**月別欠損:**

| 月 | GCS | 欠損 | 欠損率 |
|----|-----|------|--------|
| 1月 | 3,916 | 3 | 0.1% |
| 2月 | 8,083 | 3,505 | 43.4% |
| 3月 | 3,915 | 2,734 | 69.8% |
| 4月 | 4,481 | 2,588 | 57.8% |
| 5月 | 10,524 | 5,218 | 49.6% |
| 6月 | 3,748 | 2,483 | 66.2% |
| 7月 | 4,291 | 2,179 | 50.8% |
| 8月 | 7,537 | 2,960 | 39.3% |
| 9月 | 3,143 | 2,056 | 65.4% |
| 10月 | 4,606 | 2,481 | 53.9% |
| 11月 | 8,715 | 3,817 | 43.8% |
| 12月 | 3,403 | 2,371 | 69.7% |

**差分リスト**: `data/tdnet_2024_gcs_bq_gap.csv`（32,395行、doc_id/ticker/date/gcs_blob）

---

## 方針

新アーキ（`tdnet-load-daily` + `ai_processing_flow` Workflows）を使用。**2回に分割、各回 ~3.5時間**（実績ベース。当初見積 ~12h は monitor_backfill の Q並列投入を考慮していなかった）。投入タイミングはユーザーが決定。

**dedup**: `_load_processed_file_names()` が BQ `FILE_NAME` を日付範囲で取得し既存 blob をスキップ（`scripts/tdnet_load_parallel.py` L180-212）。既存 33,998 件に影響なし。

**干渉リスク**: なし（`tdnet-load-daily-daily` Scheduler PAUSED、`ai_processing_flow` Scheduler 未設定）

---

## Run 1: 2024年 上半期（1-6月） ✅ 完了

### 実績（当初見積 ~9.5h → 実績 3h34m）

| ステップ | 処理内容 | 対象件数 | 実績所要 |
|---------|---------|---------|---------|
| L1 | Load (BQ投入) | 16,498 | 1h20m |
| A1 | AI処理 Q1 (1-3月) | ~6,242 | 1h28m（Q2と並列） |
| A2 | AI処理 Q2 (4-6月) | ~10,289 | 2h14m（Q1と並列） |
| | **合計** | **16,498** | **3h34m** |

**スループット実績**: Load 206 docs/min、AI処理は monitor_backfill が Q1/Q2 を並列投入

### コマンド

```bash
# Step 1: Load 上半期（1回で6ヶ月分ロード）
gcloud run jobs execute tdnet-load-daily --region us-west1 \
  --args="--job-mode=load" \
  --update-env-vars DATE_FROM=20240101,DATE_TO=20240630,ALLOW_FULL_BACKFILL=1

# Step 2: AI処理 Q1（load完了後）
gcloud workflows execute ai_processing_flow \
  --location=us-central1 \
  --data='{"date_from":"20240101","date_to":"20240331"}'

# Step 3: AI処理 Q2（A1完了後）
gcloud workflows execute ai_processing_flow \
  --location=us-central1 \
  --data='{"date_from":"20240401","date_to":"20240630"}'
```

### monitor_backfill.py で一括実行

```bash
PYTHONUTF8=1 python scripts/monitor_backfill.py config/backfill/2024_gap_run1_h1.yaml
```

---

## Run 2: 2024年 下半期（7-12月） ✅ 完了

### 内訳（実績ベース再計算）

| ステップ | 処理内容 | 対象件数 | 推定所要 |
|---------|---------|---------|---------|
| L2 | Load (BQ投入) | ~15,864 gaps | ~1h17m |
| A3 | AI処理 Q3 (7-9月) | ~7,195 | ~1h34m（Q4と並列） |
| A4 | AI処理 Q4 (10-12月) | ~8,669 | ~1h53m（Q3と並列） |
| | **合計** | **~15,864** | **~3h10m** |

### コマンド

```bash
# Step 1: Load 下半期
gcloud run jobs execute tdnet-load-daily --region us-west1 \
  --args="--job-mode=load" \
  --update-env-vars DATE_FROM=20240701,DATE_TO=20241231,ALLOW_FULL_BACKFILL=1

# Step 2: AI処理 Q3（load完了後）
gcloud workflows execute ai_processing_flow \
  --location=us-central1 \
  --data='{"date_from":"20240701","date_to":"20240930"}'

# Step 3: AI処理 Q4（A3完了後）
gcloud workflows execute ai_processing_flow \
  --location=us-central1 \
  --data='{"date_from":"20241001","date_to":"20241231"}'
```

### monitor_backfill.py で一括実行

```bash
PYTHONUTF8=1 python scripts/monitor_backfill.py config/backfill/2024_gap_run2_h2.yaml
```

---

## 前提作業（両 Run 共通、初回のみ）

```bash
# task-timeout を 21600s (6h) に拡張（現状 3600s）
gcloud run jobs update tdnet-load-daily --region us-west1 \
  --task-timeout 21600s
```

---

## コスト見積

| 項目 | 算出根拠 | 見積 |
|------|---------|------|
| Gemma TPU (v6e-4 spot) | 4 起動 × $1 O/H + 4 × ~25min × $4/hr | ~$11 |
| Gemini Flash Batch | 決算短信 ~15% ≈ 4,860 doc × $0.00175 | ~$9 |
| Embedding | $0.025/1M chars × ~32K doc × ~2K chars | ~$2 |
| Cloud Run (load + finalize) | 6 実行 × ~2h × $0.05/vCPU-h | ~$1 |
| **合計** | | **~$23** |

---

## 完了後の後片付け

- [ ] `data/tdnet_2024_gcs_bq_gap.csv` 削除
- [x] 本プランのステータスを `✅ 完了` に更新（04/27）
- [ ] `data/logs/active_jobs.md` に gap recovery 記録追記
- [ ] task-timeout を日次運用値に戻す: `gcloud run jobs update tdnet-load-daily --region us-west1 --task-timeout 3600s`

---

## 注意事項

1. **`ALLOW_FULL_BACKFILL=1`**: 31日超の日付範囲は backfill ガードで拒否される。この env var で override
2. **`--job-mode=load`**: 新アーキでは `--args="--job-mode=load"` を使用。旧アーキの `RUN_MODE=full` は使わない
3. **AI は 3ヶ月単位**: TPU preemption リスク抑制のため。load は 6ヶ月一括で OK
4. **投入タイミング**: ユーザーが決定。推奨は平日 15:00〜24:00 JST or 週末（us-central1-b TPU spot 需要低）
5. **既存データへの影響なし**: load は FILE_NAME dedup でスキップ、Workflows は AI_STATUS='pending' のみ処理
