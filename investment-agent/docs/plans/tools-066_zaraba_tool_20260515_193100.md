# 作業計画: 066 ザラ場ツール知見MD分離（トークン費消削減）

**作成日時**: 2026-05-15 19:31 JST
**ステータス**: 完了
**分類**: (b) 継続改修型
**親知見 MD**: `docs/knowledges/tools/066_zaraba_tool.md`
**関連アイディアID**: -

## 目的

066_zaraba_tool.md（343行）を3ファイル体制に分離し、反省会・日次運用時のRead量を削減する。
059_earnings_model_eda.md + 059-1_hanseikai_log.md のパターンを踏襲。

**スコープ**: 知見MDの構造変更のみ。コード変更なし。
**非スコープ**: 未解決TODOの対応、因子ロジックの変更。

## 背景・動機

- 反省会のたびに066本体（343行）+ 066-1（88行）を全量Readする必要があり、トークン費消が過大
- 059は反省会ログを059-1に分離し、059-1に書き込みルール・運用手順を内包 → 反省会時に059本体のRead不要
- 066も同パターンにすれば、反省会時は066-1のみ（約120行）で完結

## 現状（2026-05-15 19:50 時点、反省会TODO反映後）

| ファイル | 行数 | 内容 |
|---------|------|------|
| `066_zaraba_tool.md` | 344 | 全情報が1ファイルに集約（5/15反省会でF7/基準値/is_low_profit実装済み反映） |
| `066-1_zaraba_retrospective.md` | 88 | 反省会ログ（ヘッダなし、ログのみ） |

## 目標構成

| ファイル | 想定行数 | 内容 |
|---------|---------|------|
| `066_zaraba_tool.md` | ~150 | コア（概要・サブコマンド・因子表・判定基準・運用フロー） |
| `066-1_zaraba_retrospective.md` | ~120 | 反省会ログ + YAML frontmatter + 書き込みルール + 反省会運用手順 |
| `066-2_zaraba_detail.md` | ~200 | 詳細リファレンス（データソース・キャッシュ・catchup等） |

## 作業ステップ

### Step 1. 066-2_zaraba_detail.md を新規作成

066本体から以下セクションを移設:

| 移設セクション | 現行行番号 | 行数 |
|--------------|-----------|------|
| データソース（BQテーブル一覧・コンセンサスキャッシュ・prior_data.json構造） | L72-L118 | 47 |
| 累計→Q単独変換ロジック | L120-L137 | 18 |
| キャッシュ構造 | L192-L204 | 13 |
| GCSパス参照 | L206-L211 | 6 |
| 推奨VMスペック | L228-L230 | 3 |
| 銘柄カバレッジ仕様 | L232-L241 | 10 |
| 答え合わせ・精度改善ループ（全文） | L243-L281 | 39 |
| catchup処理フロー | L283-L298 | 16 |
| XBRL予想context | L300-L307 | 8 |
| ランチャー（zara.py / zara.sh） | L309-L315 | 7 |
| PSメニュー依存パッケージチェック | L317-L336 | 20 |
| 教訓（解決済み落とし穴） | L338-L343 | 6 |
| **合計** | | **~193** |

066-2 ヘッダ:

```markdown
# ザラ場ツール 詳細リファレンス

**カテゴリ**: tools（066サブファイル）
**親知見**: `docs/knowledges/tools/066_zaraba_tool.md`

> 因子表(§スコアリング因子)・判定基準(§判定)・サブコマンド(§サブコマンド)・運用フロー(§運用フロー)は親知見を参照。
> 反省会ログは `066-1_zaraba_retrospective.md` を参照。
```

### Step 2. 066本体を縮小

**残すセクション**:

| セクション | 現行行番号 | 備考 |
|-----------|-----------|------|
| ヘッダ（ステータス等） | L1-L5 | そのまま |
| 未解決TODO | L7-L21 | そのまま |
| 関連ファイル | L22-L35 | 066-2 のポインタを追加 |
| 概要 | L36-L38 | そのまま |
| サブコマンド表 + 4箇所同時更新ルール | L40-L56 | そのまま |
| 使い方 | L58-L70 | そのまま |
| スコアリング因子表 + 判定 + 表示略称 + テーブル列 + EDA因子対応 | L140-L191 | そのまま |
| 運用フロー | L213-L226 | そのまま |

**追加するポインタ**（移設セクション跡地に1行ずつ）:

```markdown
## データソース・キャッシュ・内部仕様

→ `docs/knowledges/tools/066-2_zaraba_detail.md`（BQテーブル一覧・コンセンサスキャッシュ・prior_data.json構造・累計→Q変換・catchupフロー・XBRL予想context・教訓等）

## 答え合わせ・精度改善ループ

→ `docs/knowledges/tools/066-2_zaraba_detail.md` §答え合わせ・精度改善ループ（役割分担・精度改善サイクル・パラメータ管理。ザラ場側にverifyサブコマンドは作らない方針）

## 反省会ログ

→ `docs/knowledges/tools/066-1_zaraba_retrospective.md`（運用手順・書き込みルール・銘柄別ログ）
```

### Step 3. 066-1 に反省会運用手順を追加

059-1 のパターンを踏襲し、以下を既存ログの**前**に挿入:

**3a. YAML frontmatter 追加**:

```yaml
---
name: ザラ場ツール反省会ログ
description: ザラ場決算リアクションツールの反省会銘柄別ログ・因子改善方針の時系列記録
type: tools
作成日: 2026-04-28
更新日: 2026-05-15
ステータス: 有効
関連ファイル:
  - docs/knowledges/tools/066_zaraba_tool.md
  - docs/knowledges/tools/066-2_zaraba_detail.md
---
```

**3b. 書き込みルール追加**（059-1と同一パターン）:

```markdown
## 書き込みルール（トークン最小化）

反省会ログ追記は **Git Bash echo** を使用。**Edit ツール禁止**（old_string/new_string のトークン浪費を回避）。**PowerShell echo 禁止**（UTF-16 BOM 混入のため）。

パス: フォワードスラッシュ `docs/knowledges/tools/066-1_zaraba_retrospective.md`

### 日次ヘッダー追加
（printfテンプレート）

### 銘柄行追加
（echoテンプレート）
```

**3c. 反省会運用手順追加**:

```markdown
## ザラ場反省会の運用手順

ユーザーが「ザラ場反省会 YYYYMMDD」と指示したら:

1. 当日の results.csv を確認（ローカル `C:\tmp\zaraba_cache\YYYYMMDD\results.csv` or GCS `zaraba_scoring_results/results_YYYYMMDD.csv`）
2. 因子表の基準は 066 本体 §スコアリング因子（本ファイルには転記しない）
3. 銘柄別ログを本ファイルに追記（書き込みルールに従う）
4. 改善TODOは 066 本体 §未解決TODO に移設（本ファイルの課題セクションに初出記載→066移設）
```

**3d. 銘柄別ログの検証ルール追加**（059から移植）:

```markdown
## 銘柄別ログの検証ルール

- ユーザーがコメント・議論した銘柄 → 区分に「✓」を付与（検証済み）
- Claudeが自動生成した考察のみ → 無印（未検証）
- **テーマ・TODOとして採用するのは✓付き案件から導出されたもののみ**
```

### Step 4. INDEX.md 更新

L121-122 付近に 066-2 を追加:

```markdown
| ザラ場ツール（決算リアルタイム監視・スコアリング） | `docs/knowledges/tools/066_zaraba_tool.md` |
| ザラ場ツール反省会ログ（066サブファイル） | `docs/knowledges/tools/066-1_zaraba_retrospective.md` |
| ザラ場ツール詳細リファレンス（066サブファイル） | `docs/knowledges/tools/066-2_zaraba_detail.md` |
```

### Step 5. 066本体 §関連ファイルに 066-2 ポインタ追加

既存の `066-1_zaraba_retrospective.md` 行の直下に追加:

```markdown
- `docs/knowledges/tools/066-2_zaraba_detail.md`（詳細リファレンス: データソース・キャッシュ・catchupフロー・教訓等）
```

## 成果物

| 成果物 | 種別 |
|--------|------|
| `066-2_zaraba_detail.md` | 新規ファイル |
| `066_zaraba_tool.md` | 編集（セクション移設・ポインタ追加） |
| `066-1_zaraba_retrospective.md` | 編集（frontmatter・運用手順追加） |
| `INDEX.md` | 編集（066-2 行追加） |

## 完了条件

1. 066本体が150行以下
2. 066-1 単体で反省会ワークフローが完結（066本体の**全量Read**が不要。因子表確認は部分Read offset/limit で対応）
3. 066-2 から066本体への逆参照ポインタがある
4. INDEX.md に066-2が索引されている
5. 情報の欠落なし（066 + 066-1 + 066-2 の合計 ≒ 旧066 + 旧066-1）
6. CLAUDE.md §10 テーブルの066行が変更不要であることを確認済み（分離後も066本体がエントリポイント）

## 見積もり

- 想定所要時間: 30分
- 難易度: 低（情報の移動のみ、内容変更なし）

## 振り返り（作業後に記入）

- 実際の所要時間:
- うまくいった点:
- 改善点:
- 得られた知見:

---

## レビュー追記: 2026-05-15 19:45 JST — code-reviewer

→ `docs/reviews/179_cr_zaraba_md_split.md`
