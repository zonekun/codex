# 作業計画: 007_tob_ml_prediction.md 整備

**作成日時**: 2026-05-18 22:35 (JST)
**ステータス**: 未着手
**分類**: (c) ドキュメント整備
**親知見 MD**: `docs/knowledges/analysis/007_tob_ml_prediction.md`

---

## 目的

`007_tob_ml_prediction.md` に積み上がった 10 個の TODO を解消し、知見 MD を再び「次セッションが迷わず参照できる状態」に戻す。

---

## 現状診断（TODO 別の対処方針）

| # | TODO 内容 | 対処方針 |
|---|-----------|---------|
| 1 | バックテスト方法・結果が散在 → 別 MD に切り出す | STEP 1: `007_tob_backtest_results.md` 新規作成 |
| 2 | マスタデータ整備方法がつぎはぎ → 一から十まで整理 | STEP 3: 「データパイプライン」セクション再構成 |
| 3 | 必要な CSV・スクリプトを一か所に記載 | STEP 3: 同セクション内に表として集約 |
| 4 | TOBラベル・株主構成マスタの作り方が曖昧 | STEP 3: 同セクション内にサブステップとして明記 |
| 5 | モデル精度が最新化されていない・外出しすべき | STEP 2: `007_tob_model_metrics.md` 新規作成 |
| 6 | 「対策→結果」形式の記述の必要性吟味 | STEP 4: 設計上不要な歴史 log を削除 |
| 7 | business_partner プランから取り込み | STEP 5a: プラン精査（削除または中止クローズ） |
| 8 | 20260516 プランから取り込み（マスタの作り方） | STEP 5b: 必要部分を抽出後クローズ |
| 9 | 20260517 classify_listed_corp プランから取り込み | STEP 5c: ファイル存在確認後に対処 |
| 10 | 20260517 owner_filter_problems から取り込み（あれば） | STEP 5d: 精査後クローズ |

---

## STEP 1: バックテスト結果を外出し

### 作業内容

`docs/knowledges/analysis/007_tob_backtest_results.md` を **新規作成** し、以下の内容を移行する。

**移行元（007 本体から削除）**:
- `## ★ バックテスト結果（2022-2025、自前データ再現）` セクション全体（年度別リターン表・TOBヒット分析・TOPIX劣後分析・BACKTEST_PASS 判定根拠）
- `## バックテスト設計案`（「次のステップ」内）— 完了済みのため概要のみ残して移行

**移行先ファイル構成**:
```
# 007-BT: TOB予測モデル バックテスト結果

## 実施環境
- スクリプト・期間・評価指標の定義

## 年度別リターン（Walk-Forward 2022-2025）

## TOBヒット分析

## TOPIX比較・劣後要因

## 設計メモ（費用・リバランス・比較ベンチマーク）
```

**007 本体に残すもの**: バックテスト結果ファイルへの 1 行リンクと BACKTEST_PASS ステータスのみ

---

## STEP 2: モデル精度を外出し

### 作業内容

`docs/knowledges/analysis/007_tob_model_metrics.md` を **新規作成** し、以下の内容を移行する。

**移行元（007 本体から削除）**:
- `## 結果 → モデル精度`（旧データ: ROC-AUC 0.60〜0.75 の元論文ベース表）
- Walk-Forward 評価テーブル（22 変数 / 26 変数 / 2026-05-16 / 2026-05-18 各ラン）
- SHAP 重要度ランキング（各ラン）

**移行先ファイル構成**:
```
# 007-M: TOB予測モデル 精度推移ログ

## 精度推移サマリ（最新が上）
| 日付 | 変数数 | ROC-AUC | PR-AUC | 変更点 |

## 詳細: ラン別 Walk-Forward 結果
各ランの年別 ROC/PR-AUC + SHAP 上位

## 特徴量一覧（現行 26 変数）
```

**007 本体に残すもの**:
- 最新スコアの 1 行サマリ（`ROC-AUC 0.758 / PR-AUC 0.082 (2026-05-18)`）
- 精度ファイルへの 1 行リンク
- 特徴量カテゴリ表は概略だけ残す（詳細は精度 MD へ）

---

## STEP 3: データパイプライン手順の再構成

### 作業内容

007 本体に `## データパイプライン（初期構築 / 年次更新）` セクションを新設し、現在つぎはぎになっている手順を一本化する。

**現状の散在箇所**:
- `## 次のステップ → 株主構成データ整備` (完了扱い、手順は薄い)
- `### TOBラベル作成の実装ノウハウ` (詳細あり)
- `### IS_PAPER_TOB_LABEL 論文正解ラベル定義` (詳細あり)
- `### 株主構成データ整備 (2026-04-20 完了)` (詳細あり)
- `### 年次更新手順（SHAREHOLDER_COMPOSITION 新年度追加後）` (5 ステップ)
- `### データベース構成 (2026-05-18 更新)` (スクリプト一覧あり)

**再構成後の目標構造**:

```markdown
## データパイプライン

### A. 初期構築（一度だけ実行）

#### A-1. TOBラベル作成
スクリプト・BQ テーブル・ポイント

#### A-2. 株主構成テーブル構築
スクリプト・BQ テーブル・ポイント

#### A-3. 株主名 TYPE 分類（EXTEND テーブル）
スクリプト・TYPE 種別・現件数

#### A-4. オーナー色因子算出
スクリプト・出力カラム

### B. 年次更新（新年度データ追加時）
5 ステップを順序通りに記載

### C. スクリプト・入出力一覧
| スクリプト | 入力 | 出力 BQ | 目的 |
```

**「IS_TOB_MBO=TRUE だが IS_PAPER_TOB_LABEL=FALSE の内訳」の扱い**:
- 詳細カテゴリ分類（136件/85件/29件...）は研究上の注記として圧縮
- 将来タスク（136件救済・85件個別調査）は `## 残課題` セクションに移動

---

## STEP 4: 「対策→結果」形式の歴史記述の削除

### 削除対象（設計情報として将来生きない箇所）

以下は「試みた → 結果こうだった」の経緯 log であり、知見として定着した後は不要。

| 箇所 | 対処 |
|------|------|
| `## 次のステップ → バックテスト設計案` | STEP 1 で外出し |
| `## 次のステップ → 優先度の高い実装項目`（完了済み ~~取り消し線~~ 多数） | `git log --oneline --all \| grep -i "shareholder\|screen_tob\|tob_label"` で各項目のコミット存在を確認後に削除 |
| `## 判定と理由`（BACKTEST_PASS 判定根拠） | STEP 1 で外出し |
| `2026-04-20 初期4件テスト 抽出精度` | 手順の注意事項として 1 行に圧縮 |
| `imputation 変更後の評価 (2026-05-16)` | STEP 2 精度 MD に移行後削除 |

### 保持対象（将来の再現・保守に必要な設計情報）

- IS_PAPER_TOB_LABEL の条件定義（SQL あり）
- TOB_TYPE 判定方針（OTHER / MBO / SELF の区別）
- 他社株 TOB vs MBO の扱いの設計判断
- STOCK_CODE_LIST リーケージ問題の記載

---

## STEP 5: 参照プラン MD の精査・クローズ

### 5a. `analysis-007_tob_new_factor_business_partner_20260517_175057.md`

**状況**: `has_business_partner_investor` は `train_rf.py` から削除済み（コミット 4e811148）。
**対処**: ステータスを「中止」に更新。取り込む情報なし。

### 5b. `analysis-007_tob_ml_prediction_20260516_220000.md`

**状況**: Phase 1-4 完了、screen_tob.py 根拠表示更新のみ残とある。
**確認事項**: `scripts/tob_prediction/screen_tob.py` を Read して `TODO` 文字列が残存していないか確認する。
**「マスタの作り方」取り込み候補**: ユニクロ例のような TYPE 付与設計の背景説明 → 007 本体の「データパイプライン §A-3」コメントとして最小限取り込み。
**対処**: 確認後クローズ。

### 5c. `analysis-007_tob_classify_listed_corp_20260517_175057.md`

**状況**: ファイルが存在しない（Glob 検索で未発見）。`docs/reviews/200_cr_tob_classify_listed_corp.md` が代替の可能性あり。
**対処**: reviews/200 を確認し、追加すべき手順があれば A-3 に統合。ファイル不在の TODO は削除。

### 5d. `analysis-007_tob_owner_filter_problems_20260517_175057.md`

**状況**: 3 問題（アクティビスト誤包含・上場事業法人誤包含・非上場 HD 誤判定）の整理。
**「マスタの作り方」取り込み候補**: 候補 CSV 生成ロジックの注意事項 → STEP 3 の A-3 サブノートとして 2-3 行。
**対処**: 注意事項を 007 に移行後クローズ。

### 5e. `analysis-007_tob_owner_filter_fix_20260517_175057.md`（TODO に未記載）

**状況**: 未着手。`generate_family_holding_candidates.py`（新規）でアクティビスト・上場事業法人を除外した候補 CSV を生成するプラン。
**調査手順**: まず `scripts/tob_prediction/generate_family_holding_candidates.py` が存在するか Glob で確認する。
- 存在する → 実装済みとして中止クローズ
- 存在しない → 未実装として 007 の `## 残課題` に 1 行移動し、プランはクローズ（タスク消滅防止）

### 5f. `20260518_tob_owner_judge_batch_handoff.md`（TODO に未記載）

**状況**: STEP 1-2 完了 / STEP 3「TOB ML 予測モデルへのフィードバック（将来）」未着手。
**対処**: STEP 3 は「将来」として 007 の `## 残課題` に 1 行移行後、ハンドオフ MD をクローズ。

### 5g. `tools-081_shareholder_composition_20260420_155621.md`（TODO に未記載・情報源として必要）

**状況**: 完了済みと思われる（SHAREHOLDER_COMPOSITION テーブル構築プラン）。
**対処**: STEP 3 の「A-2. 株主構成テーブル構築」を書く際に本プランを Read して手順を照合・取り込む。取り込み後にプランのステータスを確認し、完了済みならクローズ。

---

## STEP 6: 007 本体の最終構造・コミット

### 目標セクション構成（整備後）

```
# TOB予測法: 機械学習による他社株TOB予測ポートフォリオ戦略

## 元の投資アイデア
## ★ 使用データ（特徴量概略）
## ★ 使用アルゴリズム
## データパイプライン           ← STEP 3 で再構成
## スクリーニングツール
## モデル精度（最新 1 行 + リンク）  ← STEP 2 で外出し
## バックテスト結果（BACKTEST_PASS 1 行 + リンク）  ← STEP 1 で外出し
## 既知の限界・残課題
## 関連する既存スクリプト・知見
```

### コミット単位

| コミット | 内容 |
|---------|------|
| 1 | `007_tob_backtest_results.md` 新規作成（STEP 1） |
| 2 | `007_tob_model_metrics.md` 新規作成（STEP 2） |
| 3 | 007 本体 再構成（STEP 3-4）+ TODO 全削除 |
| 4 | プラン MD クローズ（STEP 5） |

---

## 注意事項

- **破壊的操作ルール適用**: 007 本体の大規模編集は Read で現状確認 → 差分確認 → Edit の順
- **既存スクリプト・BQ テーブル名は 007 本体から変えない**: 手順整理はあくまで「同じ情報の再配置」
- **「次のステップ」欄の完了済み項目**: ~~取り消し線~~ があっても削除前に git log で実施確認
- **007_tob_backtest_results.md / 007_tob_model_metrics.md**: `docs/knowledges/INDEX.md` の `TOB ML予測モデル | 007_tob_ml_prediction.md` 行の直下に追記する
- **STEP 3 完了後**: 007 本体内の `詳細:` / `計画:` / `→` で始まる参照リンクを Grep し、移動先ファイルとの整合を確認してから STEP 4 に進む

---

## 完了条件

- [ ] 007 本体の TODO ブロック（10 行）が消えている
- [ ] モデル精度の詳細テーブルが外出しファイルに移動済み
- [ ] バックテスト結果の詳細が外出しファイルに移動済み
- [ ] データパイプライン手順が A-1〜B の順序で一本化されている
- [ ] 6 本のプラン MD がクローズマーク済み（5c はファイル不在のため TODO 削除のみでカウント外）
- [ ] INDEX.md に新規 2 ファイルが追記されている

---

## レビュー追記: 2026-05-18 23:30 JST — code-reviewer

→ `docs/reviews/209_cr_007_tob_md_cleanup_plan.md`
