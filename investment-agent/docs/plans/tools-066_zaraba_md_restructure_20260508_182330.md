# 066_zaraba_tool.md 整理・再構成プラン

**作成日時**: 2026-05-08 18:23 JST
**対象ファイル**: `docs/knowledges/tools/066_zaraba_tool.md`（493行）
**対象読者**: 次セッション担当 / code-reviewer
**目的**: 知見MD肥大化（493行）を解消し、AI可読性をC→A以上に改善する。歴史的経緯・修正履歴・運用ログを分離し、現行仕様のみに絞る。スコープ外: コード修正、因子ロジック変更、071知見MDの大幅改修
**ステータス**: ✅ 全完了（493行→313行）

**レビュー根拠**: `docs/reviews/116_mr_066_zaraba_restructure.md`（md-reviewer パターン1、AI可読性C/誤読リスクC）

---

## 整理方針

| 方針 | 内容 | 根拠 |
|------|------|------|
| A | 解決済み落とし穴5件を教訓に圧縮 | レビュー重大指摘#1 |
| B | 反省会ログを別ファイル `066-1_zaraba_retrospective.md` に分離 | レビュー重大指摘#2 |
| C | XBRL context仕様を071に集約、066はリンクのみ | レビュー重大指摘#3 |
| D | TODO/課題を冒頭「未解決TODO」に一元化 | レビュー重大指摘#4 |
| E | 設計方針セクション(20行)を圧縮 | レビュー重大指摘#5 |
| F | J-Quants移行経緯(9行)を削除 | レビュー改善提案#2 |
| G | XBRLタグ修正セクション(14行)を071集約 | レビュー改善提案#3 |
| H | VMスペック(3行)を運用フロー直後に移動 | レビュー改善提案#4 |

---

## 目標構成（整理後のH2見出し一覧）

```
目標: 493行 → 280-320行

## 🔴 未解決 TODO（先頭掲示）           -- 全TODO一元化（方針D）
## 概要                                  -- 現状維持
## サブコマンド                          -- 現状維持
## 使い方                                -- 現状維持
## データソース                          -- 現状維持（事前準備BQ / コンセキャッシュ / prior_data構造 / 当日ポーリング）
## 累計→Q単独変換ロジック                -- 現状維持
## スコアリング因子                      -- 現状維持（因子テーブル / 判定 / 表示略称 / テーブル列 / EDA因子対応）
## キャッシュ構造                        -- 現状維持
## GCS パス参照                          -- 現状維持
## 運用フロー                            -- 現状維持
## 推奨 VM スペック                      -- 移動（方針H: 旧位置から運用フロー直後へ）
## 銘柄カバレッジ仕様                    -- 現状維持
## 答え合わせ・精度改善ループ            -- 現状維持
## catchup の処理フロー                  -- 現状維持
## XBRL予想context（ザラ場固有ロジック） -- 圧縮（方針C: 確定仕様→071、ザラ場評価ルールのみ残す）
## ランチャー（zara.py）                 -- 現状維持
## PSメニュー依存パッケージチェック      -- 現状維持
## 教訓                                  -- 新規（方針A: 落とし穴5件→教訓セクションに圧縮）

削除:
  - 「解決済み: J-Quants → TDnet 移行」セクション（方針F）
  - 「設計方針: watch時のデータ全件メモリロード」（方針E: 2行に圧縮して概要に統合）
  - 「設計方針: TDnetポーリングソースの二層構成」（方針E: yanoshin非推奨注記をサブコマンド節に統合）
  - 「XBRL予想値タグ修正」セクション（方針G: 071に集約）
  - 「ザラ場反省会ログ」セクション（方針B: 066-1に分離）
  - 「将来TODO」セクション（方針D: 未解決TODOに統合）
```

---

## 作業項目

### S1. TODO一元化（方針D）

**現状**: TODOが3箇所に分散
- 冒頭「未解決TODO」: 1件（Linux VMパス検証）
- 反省会ログ内「4/28課題（未対応）」: 2件（強弱混在フラグ / コンセ比較不発）
- 末尾「将来TODO」: 3件（うち1件は完了済み取消線）

**作業**:
1. 冒頭「未解決TODO」に以下を移設・統合:
   - 反省会から: 強弱混在銘柄フラグ（P2）、コンセンサス比較未接続（P1）
   - 将来TODOから: config YAML切り出し（P2）、TDnet並行ポーリング（P2）、自社株買い分析（P2）
2. 完了済み項目（GCS保存）は削除
3. 各TODOに優先度（P1/P2）と起源日を付記
4. 末尾「将来TODO」セクション自体を削除

### S2. 反省会ログ分離（方針B）

**作業**:
1. `docs/knowledges/tools/066-1_zaraba_retrospective.md` を新規作成
   - 4/28反省会テーブル + 修正済みチェックリストをそのまま移設
   - 未対応課題2件はS1で冒頭に移設済みなので、分離先には「→ 066本体 未解決TODOに移設済み」と注記
2. 066本体から反省会ログセクション全体（L459-487、約28行）を削除
3. INDEX.mdに066-1のエントリを追加（066のサブファイルとして注記）

### S3. 解決済み落とし穴の教訓圧縮（方針A）

**現状**: 5件の落とし穴が合計約50行。全て修正済みだが現象・原因・修正の詳細が残存。

**作業**: 5件を「教訓」セクション（H2）にまとめ、以下の粒度で残す:

| 落とし穴 | 圧縮後の残し方 |
|---------|---------------|
| `fin.iloc[0]` 最古行取得 | **設計制約2-3行**: ROW_NUMBER(`DISCLOSED_DATE DESC`) + sort_values + iloc[0] の2段構え必須。片方だけでは同FY最古行を拾いF2/F4/QoQ全壊 |
| F4翌期分母 | 教訓1行: FY時の分母は実績(cumulative_op)。ガイダンス比較ではない |
| results.csv上書き | 教訓1行: watch起動時に既存results読込済み。空配列初期化禁止 |
| yanoshin遅延 | 教訓1行: 非公式APIを既定にしない。TDnet HTML公式が既定(2026-04-14〜) |
| catchup J-Quants放置 | サブコマンド間整合性の注記(L37)に統合。独立の教訓不要 |

**削除**: 落とし穴5件の詳細（現象・原因・修正の全文、L280-330）

### S4. J-Quants移行経緯の削除（方針F）

**作業**:
1. 「解決済み: J-Quants リアルタイム更新不可 → TDnet 移行」セクション全体（L248-256、9行）を削除
2. 冒頭ステータス行（L5）の「移行完了」注記で十分

### S5. 設計方針セクションの圧縮（方針E）

**現状**: 2つの設計方針セクション合計20行

**作業**:
1. 「watch時のデータ全件メモリロード」(8行) → 概要セクション末尾に1行追記:「watch起動時に全データをメモリ展開しディスクI/Oゼロでスコアリング」
2. 「TDnetポーリングソースの二層構成」(13行) → サブコマンド節のポーリング仕様に統合。yanoshin非推奨の注記は既にL99にあるため、二層構成テーブルと共通インターフェース説明は削除
3. 両セクション自体を削除

### S6. XBRL仕様の071集約（方針C + G）

**作業**:
1. 「XBRL予想値タグ修正」セクション（L353-366、14行）→ 全削除。正しいタグマッピングは071が正本
2. 「XBRL予想context限定化」セクション（L368-427、約60行）→ 以下に圧縮:
   - context扱い確定仕様テーブル（L379-387）を071に移設
   - 066には071へのリンク + ザラ場固有の評価ロジックのみ残す:
     - `ShortFOP`（短期予想）は進捗率の分母に使わない
     - `通期予想非開示` ペナルティはFY発表時のみ
     - `翌期予想非開示` は `NextYearDuration` のみ判定
   - 実装パターン定数（`TDNET_FORECAST_*_PATTERNS`）は066に残す（コード参照用）
   - 発端事例・検証ログ・コミットハッシュ（L370-378, 410-427）は全削除
3. 071にcontext扱いテーブルを追記（移設先の整合確認）

### S7. VMスペック移動（方針H）

**作業**: 「推奨VMスペック」(3行)を現在位置（catchup処理フローとXBRLタグ修正の間）から運用フロー直後に移動

### S8. 関連ファイル節の更新

**作業**:
1. L14の関連ファイル一覧に `066-1_zaraba_retrospective.md` を追加
2. 削除した計画MDへの参照がある場合は整理

---

## 作業順序

```
S1 (TODO一元化) → S2 (反省会分離) → S3 (落とし穴圧縮) → S4 (J-Quants削除)
→ S5 (設計方針圧縮) → S6 (XBRL 071集約) → S7 (VM移動) → S8 (関連ファイル更新)
→ INDEX.md更新 → git commit
```

S1→S2の順序は必須（未対応TODO移設後に反省会ログを分離するため）。S3-S8は独立。

---

## 検証

1. 整理後の行数が280-320行の範囲に収まること
2. 冒頭「未解決TODO」に全TODO（6件）が一元化されていること
3. 066-1が作成され、INDEX.mdに登録されていること
4. 071にcontext扱いテーブルが移設されていること
5. 066から071への参照リンクが正しいこと
6. 削除対象セクションが残存していないこと

---

## 関連ドキュメント

- 知見MD: `docs/knowledges/tools/066_zaraba_tool.md`（整理対象）
- 知見MD: `docs/knowledges/tools/071_xbrl_to_jquants.md`（XBRL仕様の移設先）
- レビューMD: `docs/reviews/116_mr_066_zaraba_restructure.md`
- INDEX: `docs/knowledges/INDEX.md`（066-1追加）
