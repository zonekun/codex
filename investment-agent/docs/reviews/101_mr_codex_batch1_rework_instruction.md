# MD AI可読性レビュー: Codex バッチ1再作業指示文（151社メトリクス補完 50社）

- 日時: 2026-05-07 19:42 JST
- 対象: `C:\Users\zonekun\Documents\codex\investment-agent\docs\codex-to-claude-handoff.md` L23-99（§2026-05-07 JST 151社メトリクス補完 バッチ1再作業: 50社）
- パターン: 3（ad-hoc レビュー — 作業中のMDレビュー）
- レビュアー: Claude (md-reviewer skill)
- 出力先: `docs/reviews/101_mr_codex_batch1_rework_instruction.md`
- 前回レビュー: `docs/reviews/099_mr_codex_51co_description_template_failure.md`（51社 description テンプレート失敗のパターン2レビュー）

---

## 【サマリー】

MR-099 および差し戻し通知で特定された3つの失敗パターン（全社同一タイムスタンプ、テンプレート description、ignored_count=0）を正面から攻める指示になっている。前回のバッチ1指示（L102-219）で致命的だった「下記151社セクションの手順に従う」という間接参照のみの構成が、今回は重要要件をインライン再掲する形に改善され、AI可読性は大幅に向上した。

- **AI可読性評価: A**
- **誤読リスク評価: B+**（条件付き — 下記の重大指摘1件と改善提案を反映すれば S に近づく）
- **主要リスク**:
  1. 対象50社リストが「下記セクションの対象テーブル参照」の間接参照のみ — 前回の間接参照が失敗の一因だった文脈で、再び間接参照を使うことの是非
  2. build_log カラム定義が再作業指示（L85）と差し戻し済みバッチ1（L196）で異なり、元の151社セクション（L775）とも異なる。Codex が「どのカラム定義が正か」で混乱する余地がある
  3. extraction_method 5条件チェックの記録先が extraction_notes と指示されているが、build_log カラムに extraction_method が含まれておらず、判定結果の格納先が不明確

---

## 【Markdown 品質評価】

### Accuracy / 正確性: A

- 差し戻し根拠5点（L113-119）が具体的で正確。実際の不合格パターン（同一タイムスタンプ値「2026-05-07T19:28:23」、3191の決算説明会資料問題）が引用されている
- GCS パス（L45-47）は元の151社セクション（L707-710）と一致しており正確
- description 品質基準のOK例・NG例（L58-64）がMR-099で不足と指摘されたアンチパターン明示を正確に反映
- extraction_method 5条件チェック（L70-76）が品質改善通知セクション（L470-476）と一致

### Completeness / 完全性: B+

- 前回欠落していた「実物確認のエビデンス」要件（confirmed_pdf カラム追加）が補完されている
- description 品質基準が「必須、全項目必達」と明記され、推奨 vs 義務の曖昧さが解消
- **不足**: non-tdnet(html_table) ソースのDLコマンドが作業手順L44-47に記載されていない（元の151社セクションL709には存在する）。2792 は html_table ソースだが、手順3では tdnet / non-tdnet(pdf) / download の3種のみ
- **不足**: extraction_method 判定結果の記録先。チェック結果を extraction_notes に記録（L68）とあるが、build_log には extraction_method カラムがない。判定が regex なのか gemini なのかの最終結論をどこに記録するか不明確

### Relevance / 関連性: A

- 差し戻し通知セクション（L102-219）を「差し戻し済み」status で残した上で再作業指示を先頭に配置しており、newest-first ルールに準拠。Codex が最新指示を最初に読む構成
- 冗長な背景説明がなく、失敗パターンの具体例→禁止ルール→手順→検証チェックリストの流れが明確
- 「品質改善通知」への参照が最小限に抑えられ、本文内で自己完結している（L40 の「元の151社セクションの手順と同一だが、重要箇所を再掲する」は適切）

### Actionability / 実行可能性: A-

- 「1社ずつ」が手順名・手順本文・チェックリストの3箇所で強調されており、バッチ処理への逸脱抑止が効いている
- 自己検証チェックリスト（L88-94）が前回の具体的失敗パターン5点全てに対応
- **弱点**: チェックリストが「5社ごと」という頻度指示だが、最初の5社を処理した時点でチェックを実行するトリガーがコード的に強制されない（意志依存）。ただしCodex 委託の文書指示としてはこれ以上の構造的強制は困難であり、妥当な水準

---

## 【AI 誤読リスク】

### R1. 対象50社リストの間接参照

**箇所**: `codex-to-claude-handoff.md:80`
**記述**: `前回差し戻し分と同じ50社（下記「差し戻し済みバッチ1」セクションの対象テーブル参照）。`
**誤読パターン**: Codex が「下記」を探す際、同ファイル内に複数の「対象」テーブルが存在する（L139-191の差し戻し済みバッチ1テーブル、L546-700の151社全体テーブル）。「差し戻し済みバッチ1」というセクション名で一意に特定可能ではあるが、前回バッチ1の失敗が「間接参照による指示の希薄化」に起因する文脈で、再び間接参照を使うことはリスク。
**影響度**: 低〜中。セクション名が一意なので誤読確率は低いが、AI が省力化して全体リストから50社を選ぼうとする余地がゼロではない。

### R2. build_log カラム定義の不整合

**箇所**: 再作業指示 L85 vs 差し戻し済みバッチ1 L196 vs 元151社セクション L775
**記述**:
- 再作業（L85）: `ticker, company_name, missing_count, added_count, ignored_count, confirmed_pdf, extraction_notes`
- 差し戻し済み（L196）: `ticker, company_name, missing_count, added_count, ignored_count, notes`
- 元151社（L775）: `ticker, company_name, missing_count, added_count, ignored_count, notes`
**誤読パターン**: `confirmed_pdf` と `extraction_notes` は再作業指示で新設されたカラム。差し戻し済みバッチ1の `notes` は `extraction_notes` に改名されたのか、別カラムなのかが不明。Codex が差し戻し済みセクションの Codex対応結果（L207-219）を参照して旧カラム定義で出力する可能性がある。
**影響度**: 中。カラム名の不一致は検証工程で発覚するが、再作業のコストが発生する。

### R3. 「PDFを実際に開いて読む」の実行可能性

**箇所**: `codex-to-claude-handoff.md:48`
**記述**: `PDFを実際に開いて読む`
**誤読パターン**: Codex の実行環境で「PDFを開く」がどのツール/手段で実現されるかが指示されていない。Codex が pdfplumber / PyMuPDF 等のライブラリで読むのか、GPT Vision で読むのか、Gemini Vision で読むのかの判断が委ねられている。元の151社セクションの key_constraints（L756）に「Gemini Vision使用: OK / GPT Vision使用: OK」の記載があるが、再作業指示からはこの情報への参照がない。
**影響度**: 低。Codex は前回処理で既にツール選択を行っているはずだが、再作業の文脈で手段が変わる可能性はある。

---

## 【MD 構成リスク】

### S1. 差し戻し済みセクションの Codex対応結果が残存

**箇所**: `codex-to-claude-handoff.md:207-219`
**問題**: 差し戻し済みバッチ1（L102-219）の末尾に「Codex対応結果」が残っている。このセクションは不合格成果物の記録だが、Codex が再作業時にこれを「前回の出力フォーマットの参考」として利用し、新しいカラム定義（confirmed_pdf, extraction_notes）を無視するリスクがある。
**推奨**: 「Codex対応結果」の先頭に `**⚠️ この成果物は不合格・差し戻し済み。参照しないこと。**` を追記。または status が `rejected` であることから Codex は無視すべきだが、明示的な警告が安全。

### S2. 最重要ルールと作業手順の役割分離が明確

**良い点**: 最重要ルール（L30-36）が「合格/不合格の判定基準」、作業手順（L38-54）が「具体的なアクション」と明確に分離されている。MR-099 で指摘された「義務 vs 推奨の曖昧さ」が「違反は全件不合格」の明示で解消されている。これは前回から大きく改善された点。

---

## 【指示優先順位・文脈境界】

### P1. 再作業指示と元の151社セクションの優先順位

L40 に「元の151社セクションの手順と同一だが、重要箇所を再掲する」と明記されており、再作業指示が上位指示であることが読み取れる。ただし、元の151社セクションに存在する情報（key_constraints L753-761、情報源テーブル L763-770、フィールド追加時の注意 L742-751）のうち、再作業指示で再掲されていない部分については元セクションを参照する必要がある。この「再掲部分は上位、未再掲部分は元セクションを参照」という2層構造は AI にとって理解可能だが、万が一矛盾があった場合のルールが明示されていない。

**推奨**: L40 の直後に「再作業指示の記述が元セクションと矛盾する場合、再作業指示を優先する」の1文を追加。

### P2. 品質改善通知セクションとの関係

再作業指示は品質改善通知（L435-485）の改訂1-3を実質的にインライン再掲しており、品質改善通知への参照は不要な状態になっている。差し戻し済みバッチ1（L134）に残る「品質基準: 上記「品質改善通知」セクションの改訂1〜3を適用」の記載が、Codex の読解ルートに影響を与える可能性は低い（status: rejected であるため）。問題なし。

---

## 【重大な指摘】

### #1 non-tdnet(html_table) ソースの DL コマンド欠落

**箇所**: `codex-to-claude-handoff.md:44-47`
**問題**: 作業手順のステップ3で GCS DL コマンドが3種（tdnet / non-tdnet(pdf) / download）のみ記載。対象50社の中に 2792 (`non-tdnet(html_table)`) が含まれるが、html_table 用の DL コマンドが欠落。
**AI の誤読パターン**: Codex が 2792 の処理時に「html_table ソースはどのコマンドで DL するか」を判断できない。元の151社セクション L709 には `non-tdnet(html_table): 同上（HTMLファイル）` と記載があるが、再作業指示からこの情報への導線がない。
**トリガー**: 2792 の処理時
**影響**: 2792 が DL できず処理漏れ、または誤ったパスから DL しようとして失敗
**根拠**: 対象テーブル L159 で 2792 の source は `non-tdnet(html)` と記載（差し戻し済みバッチ1）、元CSV では `non-tdnet(html_table)`
**推奨対応**: L47 の `download: 同上` の下に `- non-tdnet(html_table): 同上（HTMLファイル）` を追加
**MD修正だけで足りるか**: はい

---

## 【改善提案】

### #1 対象50社リストのインライン再掲

**箇所**: `codex-to-claude-handoff.md:80`
**現状**: 「前回差し戻し分と同じ50社（下記「差し戻し済みバッチ1」セクションの対象テーブル参照）」という間接参照
**提案**: 前回バッチ1の失敗が「間接参照のみで重要要件が希薄化した」ことに起因する文脈で、対象リストも間接参照にするのはスタイルとして不統一。ただし50社テーブルの再掲は約55行の重複を生み、ファイル肥大化のデメリットがある。対案として、L80 を以下に変更:

```
前回差し戻し分と同じ50社。一覧は下記「2026-05-07 JST (151社メトリクス補完 バッチ1: 50社 — 差し戻し済み)」セクションの「対象（50社）」テーブル（L139-191）を参照。社数・ticker は変更なし。
```

行番号を明示することで「どのテーブルか」の曖昧さを排除できる。ただし行番号はファイル編集で変動するため、セクション見出しの完全引用を主キー、行番号を補助とする記法が安全。

**期待効果**: テーブル重複なしで参照先の一意性が向上

### #2 build_log の新カラム（confirmed_pdf, extraction_notes）と元セクションとの関係明示

**箇所**: `codex-to-claude-handoff.md:85`
**現状**: 再作業指示の build_log カラムは `ticker, company_name, missing_count, added_count, ignored_count, confirmed_pdf, extraction_notes`。元の151社セクション（L775）は `ticker, company_name, missing_count, added_count, ignored_count, notes`。`notes` が `extraction_notes` に改名され `confirmed_pdf` が新設されているが、この変更の説明がない。
**提案**: L85 の後に1文追加: 「※ 前回の `notes` カラムは `confirmed_pdf`（実物確認のエビデンス用）と `extraction_notes`（extraction_method 判定理由 + その他メモ）に分離。前回カラム定義は無効。」
**期待効果**: Codex が旧カラム定義に引きずられるリスクを排除。今後のバッチ2-3でもこのカラム定義を正として参照可能

### #3 extraction_method 判定結果の格納先明確化

**箇所**: `codex-to-claude-handoff.md:66-76` と L85
**現状**: extraction_method 5条件チェックの判定結果を `extraction_notes` に記録する指示があるが、build_log に `extraction_method` カラムがない。Codex が判定した regex/gemini の最終結論は adapter JSON の `extraction_method` フィールドに反映するのか、build_log にも列として記録すべきかが不明。
**提案**: 元の151社セクション L746 に「extraction_method は変更しない（既存のregex/geminiをそのまま維持）」と記載がある。つまり extraction_method 5条件チェックは「追加フィールドの extraction_method をどうするか」ではなく「既存の extraction_method が妥当かの確認」と解釈される。この解釈が正しければ、再作業指示にも「extraction_method は既存値を維持。チェックリストは確認目的であり変更しない」と明記すべき。
**期待効果**: Codex が5条件チェックの結果に基づいて既存 extraction_method を変更する誤作業を防止

### #4 自己検証チェックリストに「3191 のような例外パターン」の言及

**箇所**: `codex-to-claude-handoff.md:88-94`
**現状**: チェックリスト5項目は前回の失敗パターンを的確にカバーしている。ただし差し戻し根拠#4（L118: 3191のソース文書が決算説明会資料で月次開示ではない）のような「PDFは存在するが月次開示ではないケース」がチェック項目にない。
**提案**: チェックリストに追加: `- [ ] DL した PDF が月次開示であることを確認したか（決算説明会資料・有価証券報告書等の誤混入がないか）`
**期待効果**: 3191 と同種のケースの早期検出

### #5 key_constraints セクション（PDF手段・GCSアップロード禁止等）への参照

**箇所**: 再作業指示全体
**現状**: 元の151社セクションの key_constraints（L753-761）には「GCSアップロード禁止」「Gemini Vision / GPT Vision 使用 OK」「GCP認証パス」等の制約が記載されているが、再作業指示にはこれらへの参照がない。
**提案**: L98（エンコーディング節）の直後に「その他の制約（GCSアップロード禁止、Vision ツール使用可否、GCP認証パス等）は下記「151社」セクションの key_constraints を参照」の1文を追加。
**期待効果**: 制約情報の発見可能性向上

---

## 【ソースコード・仕組み側への波及】

本レビュー対象は Codex への委託指示文であり、ソースコード変更は不要。

ただし、build_log のカラム定義がセクションごとに異なる問題は、バリデーションスクリプトで成果物検証時に `confirmed_pdf` カラムの存在チェックを入れることで構造的に防止可能。Claude Code 側の検証スクリプト（成果物取り込み時）に `confirmed_pdf` が空文字/欠落の行を不合格とするロジックを追加することを推奨。

---

## 【修正文案】

### 重大指摘 #1: non-tdnet(html_table) DL コマンド追加

**before** (L44-47):
```markdown
3. GCSからPDF/HTMLをDL:
   - tdnet: `gsutil cp "gs://stock_data_1930932/tdnet/{ticker}/*月次*" C:\tmp\monthly_pdf\{ticker}\`
   - non-tdnet(pdf): `gsutil cp "gs://stock_data_1930932/monthly/docs/{ticker}/*" C:\tmp\monthly_pdf\{ticker}\`
   - download: 同上
```

**after**:
```markdown
3. GCSからPDF/HTMLをDL:
   - tdnet: `gsutil cp "gs://stock_data_1930932/tdnet/{ticker}/*月次*" C:\tmp\monthly_pdf\{ticker}\`
   - non-tdnet(pdf): `gsutil cp "gs://stock_data_1930932/monthly/docs/{ticker}/*" C:\tmp\monthly_pdf\{ticker}\`
   - non-tdnet(html_table): 同上（HTMLファイル）
   - download: 同上
```

---

## 【推奨検証（Step 8）】

### 8a. 正本帰属チェック

- 推奨は全て `codex-to-claude-handoff.md` 内の編集に閉じており、正本帰属の問題なし
- memory への情報追加を推奨していない

### 8b. 上位ルール整合性チェック

- CLAUDE.md との矛盾なし。再作業指示は CLAUDE.md の `encoding="utf-8"` ルール、データ取り込み規約に準拠
- Q1: 「Codex 委託指示に実物確認のエビデンスを必須とする」ルールは CLAUDE.md に汎用ルールとして存在しない。しかしこれは Codex 固有の運用であり、CLAUDE.md に追加すべき汎用性は低い。`docs/knowledges/tools/083_codex_collaboration.md` に「成果物品質検証ポイント」として追記する方が適切
- Q2: 「委託指示文で間接参照のみ → 重要要件が希薄化する」パターンは今回が2回目（51社テンプレート失敗 + バッチ1テンプレート失敗）。083 知見 MD に「重要要件はインライン再掲、間接参照のみ禁止」ルールを追加することを推奨
- Q3: 今回の改善提案は codex-to-claude-handoff.md 固有だが、#2（カラム定義変更の明示）は他のバッチ指示にも適用すべき汎用パターン。083 知見 MD にカラム変更時の明示義務を追記推奨

### 8c. 副作用シミュレーション

**(i) 単体副作用**: 重大指摘 #1 の修正（html_table 行追加）は他の手順に影響しない。安全。

**(ii) クロスルール競合**: 最重要ルール5項目（L30-36）と作業手順8ステップ（L42-54）の間に矛盾なし。チェックリスト（L88-94）は最重要ルールの検証手段として整合。

**(iii) 状態依存シナリオ**: 該当なし。本指示は状態遷移を持たない一方向の委託指示。

**(iv) 再発防止策の実効性**:
- 「5社ごとの自己検証チェックリスト」は意志依存型だが、Codex への委託指示の文脈ではフック・バリデーターの構造的強制が困難。現実的な最善策と判断
- 「confirmed_pdf カラム」の追加は成果物側にエビデンスの痕跡を残す仕組みであり、意志依存ではなく構造的。Claude Code 側の検証で空欄検出が可能。有効

### 8d. 事後確認事項

- Codex がバッチ1再作業を返却した際、以下を優先検証:
  1. `_codex_missing_metrics_updated_at` の時刻が50社で全てバラバラか
  2. `confirmed_pdf` カラムに具体的なファイル名が記録されているか
  3. description がOK例の品質水準に達しているか（最初の5社をサンプル検証）
  4. ignored_count > 0 の社が1社以上存在するか
  5. 2792（html_table）が正しく処理されているか
- 改善提案 #3（extraction_method の扱い明確化）が反映された場合、Codex が extraction_method を変更していないことも検証対象

---

## 【確認できなかった事項】

1. Codex の実行環境で `gsutil cp` が利用可能かどうか（前回バッチ1で処理は行われたため利用可能と推定されるが、差し戻し根拠からは「PDF を DL せずにテンプレート生成した」可能性があり、gsutil の実行可否自体が未確認）
2. `data/adapter_fields_incomplete_151.csv` の 2792 の source 値が `non-tdnet(html_table)` か `non-tdnet(html)` か（差し戻し済みバッチ1テーブル L159 では `non-tdnet(html)` と表記されており、CSV 本体の値と異なる可能性がある）
3. 元の151社セクションの key_constraints に記載の Gemini Vision API キーパスが Codex 環境で有効かどうか
