# MD AI可読性レビュー: 059_earnings_model_eda.md 全体レビュー

- 日時: 2026-05-09 20:25 JST
- 対象: `docs/knowledges/tools/059_earnings_model_eda.md`
- パターン: 1（作成・更新 MD のまっさらレビュー）
- レビュアー: Claude (md-reviewer skill)
- 出力先: `docs/reviews/144_mr_059_earnings_model_eda.md`

---

## 【サマリー】

決算反応モデルの EDA・予測・答え合わせ・反省会に関する包括的な知見ファイル。スコアリング因子テーブル、CLI コマンド、GCS保存先、反省会運用手順、改善TODO等を網羅する約750行の大規模MD。

- **AI可読性評価: B**
- **誤読リスク評価: C**
- **主要リスク**:
  1. 削除済みファイル（`batch_rerun_predict.py`, `earnings_model_predict.ipynb`）への参照が残存し、AI が存在しないファイルを操作対象と誤認する
  2. 古い状態記述（「F4 コンセンサスは現在一時除外中」「5因子スコアリング」）が現行仕様と矛盾し、AI がコード変更時に誤った前提で作業する
  3. 反省会ログ（約300行）と改善方針の未実装分析が現行仕様セクションと混在し、AI がどれが現行ルールでどれが検討中の構想かを区別しにくい

---

## 【Markdown 品質評価】

### Accuracy / 正確性: C

- **削除済みファイルへの参照が5箇所**: `batch_rerun_predict.py`（L187, L192）、`earnings_model_predict.ipynb`（L220, L312, L563）。いずれも commit `d732c70` で削除済み。AI がこれらのファイルを探索・参照・修正しようとして失敗する
- **PRED_COLUMNS 列数が不正確**: L180 で「34列」と記載されるが、実際のコードでは 35列（`fy_achievement` が追加後に列数更新漏れ）
- **「F4 コンセンサスは現在一時除外中」（L685）が虚偽**: `compute_score()` (core.py L76-101) で F4 は完全に稼働中。EDA ノートブック初期の除外状態が残存
- **「5因子スコアリング」（L690, L692）が古い**: 現行は 16因子。クラスタ別精度分析セクション（2026-04-04 分析日）の時点情報が更新されていない
- **L220 の同期ルールが無効**: 「`compute_score()` は `earnings_model_predict.ipynb` Cell7 と完全一致する必要がある」-- このノートブックは削除済み。`predict.py` が `earnings_model_core.py` を import する現行構成では、core.py が Single Source of Truth

### Completeness / 完全性: B+

- CLI コマンド、GCS 命名規約、反省会手順、因子テーブル等、運用に必要な情報は概ね揃っている
- `predict.py` の内部動作（BQ クエリ → 特徴量計算 → `compute_score()` → GCS 保存）のデータフロー全体像が暗黙。CLI サブコマンドの使い方は明確だが、新因子追加時にどのファイルのどこを修正すべきかの手順が明示されていない（core.py? predict.py? 両方?）
- `hanseikai_notebook.ipynb` がディスク上に存在するが MD に記載なし

### Relevance / 関連性: C

- **反省会ログ（L267-L515, 約250行）が巨大**: 5回分の銘柄別ログ・改善方針が知見ファイルの3分の1を占める。AI がこの MD を読む際、反省会ログの個別銘柄考察を現行ルールと混同するリスクがある
- **因子改善TODO テーブル（L119-L153）に「実装済」と「未着手」が混在**: 実装済みは因子テーブル本体（L93-L116）に反映済みのため、TODO テーブルの「実装済」行は冗長。一方「サンプル蓄積待ち案件」は研究ノートとしての価値があるが、運用指示と混在している
- **「モデル改善候補」テーブル（L714-L724）が因子改善TODO テーブルと重複**: 同じ改善案が2箇所に異なる形式で記載されている（例: 黒字転換ファクター、事前騰落減衰）

### Actionability / 実行可能性: B

- CLI コマンド例が具体的で、実行パス・環境変数も明記されている
- 反省会の3ステップ手順が明確（L224-L265）
- ただし「因子を変更したらこのテーブルも同時に更新する」（L89）等の同期義務は明記されているが、更新すべき箇所が分散している（因子テーブル、因子改善TODO、モデル改善候補、設計ノート）

---

## 【AI 誤読リスク】

### R1. 削除済みファイルの操作対象化

- **箇所**: L187-L222
- **AI の誤読パターン**: 「batch_rerun_predict.py のスコアリングロジックを core.py と同期させる」「earnings_model_predict.ipynb の Cell7 を更新する」等の作業指示を生成する
- **トリガー**: ユーザーが「因子を追加して」「スコアリングを変更して」と指示した場合
- **影響**: 存在しないファイルの編集を試みてエラー → 代替ファイルを探す際に誤ったファイルを選択する可能性

### R2. 「F4 一時除外中」に基づく誤判断

- **箇所**: L685
- **AI の誤読パターン**: F4 コンセンサス乖離が無効化されていると判断し、F4 関連のバグ修正やデータ更新を不要と判断する
- **トリガー**: ユーザーが「モデル設計の原則を確認して」と指示し、AI が L683-L688 を参照した場合
- **影響**: F4 のバグを見逃す / F4 のデータ更新を省略する

### R3. 反省会ログの未実装案を現行仕様と混同

- **箇所**: L326-L401（テーマC/D/E/F/G/H/I/J の設計案）
- **AI の誤読パターン**: 「テーマF FY予想未達ペナルティ」の設計案（L431-L439）を読み、「F5連動: F15発火時はF5スコアを半減」を現行仕様と判断してコードに実装する
- **トリガー**: 因子追加・修正タスク時にこの MD を参照した場合
- **影響**: 検討段階の設計案をコードに直接実装 → テスト不十分で本番投入

---

## 【MD 構成リスク】

### S1. 情報の時系列混在

ファイル全体が「現行仕様」「反省会ログ（過去の分析メモ）」「未実装の設計案」「廃止されたファイルの記述」の4種類の情報を区別なく混在させている。見出し階層は適切だが、各セクションが「現行」か「過去」か「検討中」かを示す明示的なステータスマーカーがない。

### S2. 同一情報の複数箇所記載

因子に関する情報が以下4箇所に分散:
1. スコアリング因子クイックリファレンス（L93-L116）-- 現行仕様
2. 因子改善TODO（L119-L153）-- 完了済み + 未着手混在
3. 各反省会の改善方針（L326-L515）-- 検討中の詳細設計
4. モデル改善候補テーブル（L714-L724）-- 未着手の候補リスト

AI が因子の現行仕様を確認する場合に4箇所を読む必要があり、矛盾が生じやすい。

### S3. ファイルサイズ過大（約750行、26000+ token）

AI のコンテキストウィンドウを大きく消費する。反省会ログだけで約250行。「反省会で使う知見」と「反省会の記録」が同一ファイルにあるため、日常的な参照（predict 実行時）でも反省会ログ全体がコンテキストに載る。

---

## 【指示優先順位・文脈境界】

- CLAUDE.md の高頻度参照テーブルから「決算反応モデル EDA・予測」「決算答え合わせ・反省会」の2エントリでこの MD に誘導される。参照導線は適切
- `docs/knowledges/INDEX.md` L79 に `earnings_model_predict.ipynb` への参照が残存（削除済みファイル）。INDEX.md も更新が必要
- `docs/knowledges/INDEX.md` L81 に `batch_rerun_predict.py` への参照が残存。同上
- `066_zaraba_tool.md` との責務分担は L676-L679 で明記されており適切

---

## 【重大な指摘】（即修正）

### #1 削除済みファイルへの参照残存（5箇所）

- **箇所**: `059_earnings_model_eda.md:187`, `:192`, `:220`, `:312`, `:563`
- **問題**: commit `d732c70`（「CLI統一 predict.py新規 + notebook/batch削除」）で `batch_rerun_predict.py` と `earnings_model_predict.ipynb` が削除されたが、MD の記述が更新されていない
- **AI の誤読パターン**: 因子変更タスクで「earnings_model_predict.ipynb Cell7 と完全一致させる」同期ルール（L220）に従い、存在しないノートブックを探索する
- **トリガー**: 因子追加・スコアリング変更の全タスク
- **影響**: 作業遅延、誤ったファイルへの修正
- **根拠**: `git log --oneline --all -- scripts/earnings_model/batch_rerun_predict.py` で削除確認済み。ディスク上にも不在
- **推奨対応** [検証済み]:
  - L187-L222 の「バッチ再実行」セクション全体を削除または「**廃止: predict.py backfill サブコマンドに統合済み**」と明記
  - L220 の同期ルールを「`compute_score()` は `earnings_model_core.py` が Single Source of Truth。`predict.py` は core.py を import して使用する」に更新
  - L563 の「predict: 11セル」を削除（predict はノートブックではなく CLI スクリプト）
  - L312 の反省会ログ内の参照は履歴として残してよいが、冒頭に `(当時。現在は predict.py に統合)` を付記
- **MD修正だけで足りるか**: はい。INDEX.md（L79, L81）も同時に更新が必要

### #2 「F4 コンセンサスは現在一時除外中」が虚偽

- **箇所**: `059_earnings_model_eda.md:685`
- **問題**: L685 に「ただし F4 コンセンサスは現在一時除外中」とあるが、`earnings_model_core.py` L76-101 で F4 は段階的スコア（+3/+2/+1/-1/-2/-3/-4/-5）として完全稼働中。因子テーブル（L94, L98）にも F4 は記載済み
- **AI の誤読パターン**: モデル設計原則セクションを参照し、F4 が無効化されていると判断 → F4 関連のデータ更新・バグ修正を省略
- **トリガー**: 「モデルの設計を確認して」「原則を教えて」等のタスク
- **影響**: F4 のバグ見逃し、データ更新省略
- **根拠**: `earnings_model_core.py` L76-101 の実コードで確認
- **推奨対応** [検証済み]: L685 の括弧書き「（ただし F4 コンセンサスは現在一時除外中）」を削除
- **MD修正だけで足りるか**: はい

### #3 「5因子スコアリング」が古い

- **箇所**: `059_earnings_model_eda.md:690`
- **問題**: L690 のセクション見出しに「5因子スコアリング」とあるが、現行は16因子（`compute_score()` の docstring にも「16因子」と明記）
- **AI の誤読パターン**: クラスタ別精度分析を参照する際、「5因子」を現行の因子数と誤認 → 精度数値を現行モデルの精度と混同
- **トリガー**: 精度評価・モデル改善計画のタスク
- **影響**: 過去の精度数値（方向一致率 0.407, 相関 0.176）を現行精度として報告
- **根拠**: `earnings_model_core.py` L34 の docstring「16因子で決算反応スコアを算出する」
- **推奨対応** [検証済み]: L690 を「残差相関モデル × 16因子スコアリング」に修正し、L692 に「**注: 以下は 2026-04-04 時点（5因子版）の分析。現行16因子版の精度は反省会ログを参照**」を追記
- **MD修正だけで足りるか**: はい

### #4 PRED_COLUMNS 列数の不一致

- **箇所**: `059_earnings_model_eda.md:180`
- **問題**: 「PRED_COLUMNS: GCS prediction JSON の保存カラム一覧（34列）」とあるが、実際は 35列（`fy_achievement` 追加後の更新漏れ）
- **AI の誤読パターン**: JSON パース時やカラム数の検証ロジックを書く際に 34 を期待値として使用
- **トリガー**: GCS データ読み込み・パース処理の実装時
- **影響**: 検証ロジックの誤作動（軽微だが正確性の問題）
- **根拠**: `earnings_model_core.py` L18-30 で実測 35列
- **推奨対応** [検証済み]: L180 の「34列」を「35列」に修正
- **MD修正だけで足りるか**: はい

---

## 【改善提案】（中優先度）

### #1 反省会ログの分離

- **箇所**: L267-L515（約250行）
- **現状**: 5回分の反省会ログ（銘柄別テーブル・改善方針詳細）が知見ファイル本体に埋め込まれている。ファイル全体の1/3を占め、AI のコンテキストを大量消費する
- **提案**: 反省会ログを別ファイル（例: `059-1_earnings_review_log.md`）に分離し、本MDにはポインタのみ残す。「反省会の運用手順」セクション（L224-L265）は本MDに残す
- **期待効果**: 日常的な predict 実行や因子確認時のコンテキスト消費を削減。反省会ログ自体は参照可能

### #2 因子改善TODO の整理

- **箇所**: L119-L153
- **現状**: 「実装済 (2026-05-03)」が7件、「設計中」が3件、「保留」が1件が混在。実装済み項目は因子テーブル本体（L93-L116）に既に反映済みで冗長
- **提案**: 実装済み項目を折りたたみ（`<details>` タグ）にするか、実装履歴セクションに移動。TODO テーブルには未着手・設計中のみ残す
- **期待効果**: AI が「次に何をすべきか」を即座に判断できる

### #3 モデル改善候補テーブル（L714-L724）と因子改善TODO の統合

- **箇所**: L714-L724
- **現状**: L714-L724 のテーブルと L119-L153 の TODO テーブルに同じ改善案が異なるフォーマットで記載されている（例: 黒字転換ファクター=L718 と L133 で重複）。ステータスも不整合（L718 は「未着手」だが L133 は「実装済 (2026-05-03)」）
- **提案**: L714-L724 を削除し、因子改善TODO テーブルに一本化する
- **期待効果**: 改善候補の一元管理。ステータス不整合の解消

### #4 反省会ログ内の未実装テーマ（C-M）の整理

- **箇所**: L326-L515 の各テーマ（C, D, E, F, G, H, I, J, K, L, M）
- **現状**: 実装済みテーマ（C低ベース, F/F16未達, I/PEG拡張, J/EPS追加）と未着手テーマ（D, E, K, L, M）が反省会ログ内に散在。未着手テーマの設計案は因子改善TODO にもリストされているが、詳細設計は反省会ログ内にしかない
- **提案**: 未着手テーマの設計案を因子改善TODO テーブルの補足として集約するか、反省会ログ分離時に一緒に整理する
- **期待効果**: AI が未着手因子の設計案を探す際の導線が明確になる

### #5 `hanseikai_notebook.ipynb` の記載追加

- **箇所**: 該当なし（記載漏れ）
- **現状**: `scripts/earnings_model/hanseikai_notebook.ipynb` がディスク上に存在するが、MD に記載がない
- **提案**: ノートブック起動・保存先セクション（L556-L561）に追記
- **期待効果**: ファイルの用途・関係が明確になる

---

## 【ソースコード・仕組み側への波及】

MD修正で全て対応可能。ソースコード側の変更は不要。

ただし、INDEX.md にも削除済みファイルへの参照が残存しているため、INDEX.md も同時に修正が必要:
- L79: `earnings_model_predict.ipynb` への参照を `predict.py` に更新
- L81: `batch_rerun_predict.py` への参照を `predict.py backfill` に更新

---

## 【修正文案】

### 指摘#1 L220 の同期ルール修正

**before** (L220):
```
**スコアリングロジック**: `compute_score()` は `earnings_model_predict.ipynb` Cell7 と**完全一致**する必要がある。ノートブックの因子を変更したらこのスクリプトも同時更新する。
```

**after**:
```
**スコアリングロジック**: `compute_score()` は `earnings_model_core.py` が Single Source of Truth。`predict.py` は core.py を import して使用するため、因子追加・閾値変更は core.py の `compute_score()` を1箇所修正するだけで反映される。
```

### 指摘#2 L685 の削除

**before** (L685):
```
3. **YoYとコンセンサスサプライズは別の情報**。YoYが良くてもコンセンサスを下回れば売られるし、逆もある（ただし F4 コンセンサスは現在一時除外中）
```

**after**:
```
3. **YoYとコンセンサスサプライズは別の情報**。YoYが良くてもコンセンサスを下回れば売られるし、逆もある
```

---

## 【推奨検証（Step 8）】

### 8a. 正本帰属チェック

- 指摘#1 の推奨は `059_earnings_model_eda.md`（知見MD）と `INDEX.md` の修正。いずれも正本の所在として適切
- memory への書き込みは推奨していない。CLAUDE.md 違反なし

### 8b. 上位ルール整合性チェック

- CLAUDE.md §知見ファイル整合義務:「既存スクリプトの動作・出力・依存関係を変更した場合、対応する知見ファイルの記述が実態と整合しているか確認・更新すること」-- 今回の指摘は、過去のスクリプト変更（predict.py 統合）に伴う知見ファイル更新が漏れていた事象。CLAUDE.md の既存ルールに合致
- Q1: CLAUDE.md に「削除済みファイルの参照を残さない」汎用ルールは存在しない。§知見ファイル整合義務が包含しているため、追加は不要と判断
- Q2/Q3: 今回の問題パターン（スクリプト統合・削除後のMD更新漏れ）は §知見ファイル整合義務の適用範囲内。個別MD固有のfixで十分

### 8c. 副作用シミュレーション

**(i) 単体副作用**: 推奨修正は削除済みファイルの参照除去と事実誤認の修正のみ。新たな誤読リスクは生じない

**(ii) クロスルール競合**: L89 の「因子の追加・閾値変更・ウェイト変更・廃止を core.py に反映したら、このテーブルも同時に更新する」ルールと、修正後の L220（core.py が SSoT）は整合する。競合なし

**(iii) 状態依存シナリオ**: 該当なし

**(iv) 再発防止策の実効性**: 本レビューの推奨は事実の修正であり、再発防止策（意志依存型の注意喚起）ではない。CLAUDE.md §知見ファイル整合義務が構造的強制として既に存在する

### 8d. 事後確認事項

- 修正実施後、`predict.py` の CLI ヘルプと MD のコマンド例が一致しているか確認
- INDEX.md の修正後、CLAUDE.md の高頻度参照テーブルのリンク先が正しいか確認（現在は正しい）
- 次回の因子追加時に、因子テーブル・因子改善TODO・設計ノートの3箇所が同時に更新されるか観察

### 8e. 出力内部整合性チェック

**(i) 指摘統合チェック**: 指摘#1（削除済みファイル参照）は5箇所だが同一根本原因（commit d732c70 後のMD更新漏れ）。1つの重大指摘に統合済み。指摘#2, #3, #4 はそれぞれ独立した事実誤認であり、統合不可

**(ii) 指摘-修正文案クロスチェック**: 修正文案は指摘#1 と #2 に対応。指摘#3, #4 は修正内容が自明（文字列置換）のため文案不要

**(iii) 技術的正確性チェック**: 推奨は MD の文言修正のみで技術的内容を含まない

---

## 【確認できなかった事項】

- 因子改善TODO の実装済みステータスと `compute_score()` のコード内容が全項目で完全一致しているかの詳細照合（F15 の `is_turnaround` 等は確認済みだが、全16因子の閾値・条件の完全照合は未実施）
- 反省会ログ内の個別銘柄データ（スコア値・騰落率等）の正確性検証
- GCS 保存先パスの実在確認（MCP ツールによる GCS 参照が必要）
- `hanseikai_notebook.ipynb` の用途・`hanseikai_summary.py` との関係
