# レビュー提出: orders ソルジャー B+C ハイブリッド方式改修プラン

- 日時: 2026-05-19 00:20 JST
- 提出者: メインエージェント
- レビュー対象: `docs/plans/refactor_orders_soldier_table_extraction_20260519_001056.md`
- 改修対象スキル: `skills/orders_soldier.md`
- 実装 commit: `5ea7ed39` (feat: orders ソルジャー B+C ハイブリッド方式)
- レビューパターン: **パターン 2**（既存コード改修プランのレビュー、`_template_refactor.md` フォーマット）

---

## 事象・背景

2026-05-18/19 の resume 実走（5 件）で、現行 spec の「PyMuPDF 経路一本化」のトークン削減効果が限定的であった（平均 85,468 token、偽陽性ゼロのため skip が発火せず Read tool で PDF 全文投入が発生）。
トークン削減を進めるため、Read tool を実質スキップし、PyMuPDF で抽出した text + 表 JSON を assistant に直接渡す **B+C ハイブリッド方式** へ移行する。あわせて spec 内の CHUNK_TEXT 関連記述を削除し、PyMuPDF 一本化を徹底する。

## ユーザー判断確定事項

- B+C ハイブリッド: ヒットページごとに (b) ページテキスト全文 + (c) `page.find_tables()` で表 JSON 化 を両方 assistant に渡す
- 純粋 C 案だと文章記述（「受注残高は前年同期比で大幅に増加」等）が漏れるため、ハイブリッドで取りこぼし防止
- 月次開示の既存実装と整合（仕組み統一）
- CHUNK_TEXT 関連は spec から削除（PyMuPDF 一本化徹底、`CHUNK_INDEX IS NOT NULL` 罠回避は BQ 一般注意として残す）

## 補足情報

- 直前の改修 commit: `0d407011`（PyMuPDF 一本化 + 既知問題是正） / `6e762b33`（キーワード追加 受注*推移）
- 前回レビュー: `docs/reviews/210_cr_orders_soldier_pymupdf_plan.md`（PyMuPDF 一本化版）
- 関連実装パターン: 月次開示パイプラインで PyMuPDF `find_tables()` + テキスト併用方式が運用実績あり
- コマンダー spec (`skills/orders_commander.md`) は変更不要（CHUNK_TEXT 関連 grep 結果ゼロ、表抽出はソルジャー内部実装で完結）

## 重点観点

1. **B+C ハイブリッド方式の妥当性**: 純粋 B / 純粋 C ではなく両方渡す設計に副作用・取りこぼしの新規リスクはないか
2. **トークン削減の現実性**: `pages_data` をまるごと JSON 化して assistant が Read する設計が、本当に Read tool 直読より軽量と言えるか（表 JSON のサイズ感）
3. **`page.find_tables()` 失敗時の取りこぼし**: 罫線なし表・複雑レイアウトで `tables=[]` になった場合、text に残るが d[] 構築指針が混乱しないか
4. **§Step 6 数値抽出指針の明確性**: 表優先 / text フォールバックの順序が、複数表/複数 kind 混在のケースで曖昧にならないか
5. **CHUNK_TEXT 削除の副作用**: `CHUNK_INDEX IS NOT NULL` 罠記述を独立化したことで、将来の読者が両罠を取り違えないか
6. **既存 7 フィールド・対応アンチパターン表・検証戦略 4 段の完備性**

---

## レビュー追記欄

（以下、レビュアー記入欄。`/code-reviewer` パターン 2 による追記）

---

## レビュー追記: 2026-05-19 00:55 JST — code-reviewer (パターン2)

- 日時: 2026-05-19 00:55 JST
- 対象プラン: `docs/plans/refactor_orders_soldier_table_extraction_20260519_001056.md`
- 対象スキル: `skills/orders_soldier.md`（commit 5ea7ed39 で本プラン相当の改修実装済）
- レビュアー: Claude (code-reviewer runbook)
- 出力形態: ユーザー指示により新規 `docs/reviews/NNN_cr_*.md` は作成せず、本 212 MD に直接追記

### 【サマリー】

- 変更の要約: PyMuPDF 抽出時にヒットページの `page.find_tables()` 結果も JSON へ同梱し、assistant が `pages.json` 1 回 Read で text+tables を取得して `d[]` を構築する B+C ハイブリッド方式に移行。CHUNK_TEXT 関連の旧記述を削除。
- 品質評価: **B** — 設計の方向性（Read tool 直読を pages.json に置き換えてトークン削減）は妥当だが、**前提に事実誤認 1 件（重大）** あり。実装側 (`skills/orders_soldier.md` commit 5ea7ed39) はプランより踏み込んだ整備が進んでおり、プラン MD の記述が実装にやや遅れている部分がある。
- 主要リスク（上位3件）:
  1. プラン §設計上の重要決定 3 / 前提サマリ §関連 incident に書かれた「月次開示パイプラインで PyMuPDF `find_tables()` + テキスト併用が運用実績あり」という前提が **事実と異なる**（実装は pdfplumber 主力、PyMuPDF `find_tables()` はセクション識別補助のみ）
  2. 大型決算短信（30-50 ページ）で hit_pages が広がった場合、`pages_data` JSON が肥大化し assistant 1 回 Read でも数万 token を消費して削減効果が頭打ちになる可能性。プランに**サイズ上限ガード**が無い
  3. `tbl.extract()` の戻り値（結合セル null・改行混入セル）に対する Step 6 の取扱規約が無く、assistant 側で解釈が割れて `r` 行の数値・行名対応が崩れる可能性

### 【改修プラン評価】

#### 妥当性（独立仮説 → 方向性照合）

**独立仮説（プラン提案を一旦脇に置いた根本原因推定）**:
- 偽陽性ゼロ実走の 85,468 token は「Read tool が PDF 全文をそのままトークン化して LLM に投入する」のが主因と推定できる。これに対する根本対処は (A) LLM 投入対象を「ヒットページ＋小コンテキスト」に絞る、(B) Read tool を回避し既に抽出済のテキストを assistant に渡す、のいずれかで成立する
- プランの提案（pages_data に絞り込み済 text + tables を入れて Read tool を JSON 1 回に圧縮）は **(A)+(B) を同時に満たす設計**であり、対症療法ではなく根本対処として妥当。「Read tool を別の Read tool に置き換えただけ」ではないかという懸念は、対象が「PDF バイト列の OCR/抽出を含む全文」から「事前抽出済の hit_pages 限定 text + tables」に変わる点で量的に大きく異なるため、構造としては前進と評価する

**方向性照合**: プランの修正方針は推定根本原因と整合。ただし下記 §抜け漏れ の通り、ヒットページ数とページ密度に対する上限ガードが無いため、ロングテール（巨大決算短信）で削減効果が劣化する可能性は残る。

**対症療法パターン検出**: 該当なし（try/except で症状吸収・特定条件ワークアラウンド型ではなく、データフロー自体の再設計）。

#### 副作用・デグレードチェック

- [x] **`page.find_tables()` の精度不足リスク（重大）**: `docs/knowledges/tools/062_pdf_processing_strategy.md` L105 に「PyMuPDF (fitz) の `find_tables()` は pdfplumber より弱く、複雑表で崩れる」と明記されており、プランの「月次開示で運用実績あり」前提は**該当箇所を実コードで確認すると正しくない**。`scripts/extract_monthly_data.py` L1717 は `pdfplumber.open(...)` → `page.extract_tables()` が主力、PyMuPDF `find_tables()` は L1746・L2156 で**テーブル直上のセクションラベル取得のための bbox 取得**にしか使われていない。表データ抽出本体は pdfplumber 側。→ プラン側に「PyMuPDF の表認識は弱いため、表が崩れた場合は text フォールバックで救済する」旨の明示が必要（Step 6 §数値抽出の優先順位 §2 「表がない / 表に該当数値が無い場合は text から抽出」と書かれているが、「**表があっても崩れていれば text を採用**する」判定が明示されていない）

- [x] **`tbl.extract()` 戻り値仕様の取扱欠落**: `tbl.extract()` は 2 次元配列を返すが、結合セル・空セルは `None` または `""`、改行を含むセルは `"行A\n行B"` のような複数行文字列になる。プラン Step 6 §数値抽出の優先順位は「表があれば行/列対応が確実」とだけ書いているが、これら表現揺れに対する正規化指針（None→null、改行除去、空白圧縮、数値変換の `,`/`△`/`-` 等）が無い。assistant が各自解釈すると 5 件平均 token は減っても**抽出精度（DOCS_WITH_DATA 数）が落ちる**可能性。実装側 (`skills/orders_soldier.md` L406-410) に「マイナスは `-数値`（△ は使わない）」「欠損値は `null`」と既に書かれているため、これと `tbl.extract()` 出力を結ぶ正規化ステップを Step 6 に追記すべき

- [x] **「`pages_data` のサイズが Read tool 1 回で読めるか」のサイズ感未検証**: プランは「JSON 自体が PDF 全文より桁違いに軽い」と断言しているが、実測値の根拠が無い。日本語 1 ページのテキスト ≒ 1,500-3,000 文字 ≒ 約 500-1,500 token、加えて表の各セル文字列を JSON 化すると行×列分膨らむ。受注高開示で 6 PDF × 平均 3 ヒットページ = 18 ページ分が `pages_data` に乗ると、楽観値でも 9,000-27,000 token、悲観値で 50,000+ token に達し得る。**Read tool 直読の 85,468 token から「桁違いに軽い」とまで言えるかは未検証**。smoke test の検証戦略に「pages.json の最大バイト数を 1 ticker ごとに ntfy 報告して上限ガードの要否を見る」観点を加えるのが望ましい

- [x] **画像 PDF fallback 閾値 500 文字の妥当性**: プラン §修正方針 1) Step 5 表で「`total_chars < 500` なら画像 PDF fallback」としているが、根拠が無い（実機サンプル何件で 500 を境にスキャン判定が妥当か未提示）。受注高ソルジャーが扱うのは「決算短信＋決算説明資料＋受注速報」で、最低でも 8-10 ページの文書が大半。500 文字未満で **本物のテキスト PDF が誤って fallback 行きになる**ケースは少ないと推定できるが、逆に「PyMuPDF で 600 文字程度取れるがほぼ全部ヘッダ・ノイズ」の準スキャン PDF を**普通の hit_pages 空＝偽陽性として skip してしまう**側のリスクは残る。妥当性の根拠（過去サンプルでの 500 文字境界の分布）をプランに 1 行追記しておくと将来の調整時に役立つ

- [x] **CHUNK_TEXT 関連削除の副作用**: §Step 2 SQL コメント（旧 L104）の削除は安全。§注意事項（旧 L603）も「CHUNK_INDEX IS NOT NULL を付けない」という核心の罠記述は保持されているため後退なし。**ただし `docs/knowledges/tools/059_earnings_model_eda.md` 等の他知見が「orders_soldier は CHUNK_TEXT を補助検索に使う方針」だと書いていないか**プラン提出側で grep 確認した形跡が無い。Grep の結果（本レビュー側で確認）では他ファイルに記載なしのため実害なしだが、プラン MD §検証に「他知見 MD への波及確認」を 1 行追加するのが望ましい

- [x] **過去緩和策の剥がし有無**: コメント削除だけのため、過去 incident で入った緩和コードを剥がす操作は無い。`pages.json` 中間ファイル経由化により、§Step 7 PDF 削除時の cleanup glob (`/c/tmp/tdnet_orders/{ticker}_*`) に `*.pages.json` が含まれることはプランで明示されており、ゴミ残しリスクは抑えられている

#### 抜け漏れ（類似観点での横展開含む）

- [ ] **`pages_data` 上限ガードの未定義**: 30-50 ページの巨大決算短信＋ヒットページ過剰検出（受注関連が文書全体に散らばる業種）で `pages_data` が爆発し得る。「hit_pages 数が一定（例: 10 ページ）を超えたら表抽出のみに絞る」「`pages_data` バイト数が N MB を超えたら警告」等の上限ガードがプランに無い。観点だけでも Step 4-B か Step 5 のどこかに 1 行入れるのが望ましい

- [ ] **表抽出時間の上限ガード未定義**: `page.find_tables()` は罫線解析を行うため、複雑なレイアウトページで秒オーダの時間がかかる。6 PDF × 数十ページ × find_tables = 数十秒〜数分の追加コストがハートビート 30 分上限を圧迫する可能性。プランの検証戦略に「smoke test で wall time の改修前後比較」を追加するのが望ましい（現状は token 比較のみ）

- [ ] **複数表 / 1 表複ページにまたがる場合の指針**: 1 ページに表が 2 つ以上ある（例: 「受注高（金額）」と「受注高（数量）」が並ぶ）場合、プラン §数値抽出の優先順位 §3 で「§抽出対象に該当する表だけを採用」と書かれているが、判定ロジックは assistant 側に丸投げ。`tbl.extract()` の 2 次元配列の中身を見ないと「これは受注高表」「これは売上 P/L 表」が判別できない。assistant 解釈に幅が出る箇所のため、Step 6 に「1 ページ複数表のときは各表の見出し行（先頭セル）に §抽出対象キーワードがマッチするものだけ採用」のような具体規約を追加するのが望ましい

- [ ] **横展開: コマンダー側のログ仕様**: pages.json サイズが各 ticker でどの程度になるかを `orders_log.tsv` に追記する観点が無い。本改修の効果検証のため、ソルジャー Step 7 §status file に `PAGES_JSON_BYTES=...` を 1 行追加してコマンダー側でロギングできるようにすると、後段の最適化判断に効く（妥当性: コマンダー spec の status file パース仕様も追従が必要なので、本プランのスコープ外として記録だけしておくのが現実的）

- [ ] **smoke 3 件の銘柄選定根拠**: プラン §検証戦略 1 で 1734・1736・1739 を選んでいるが、これらが「PDF 数 6 件揃い・偽陽性ゼロ・大型短信」の代表ケースか不明。「token 削減量が最も効きそうな 3 件」（hit_pages が広がる業種 / 巨大短信 / pages 多めの説明資料がある銘柄）を 1 件以上含めるのが効果検証の点で望ましい

- [ ] **211 / 210 レビューでの未解決事項の継承確認**: 直前の 210 レビュー（`docs/reviews/210_cr_orders_soldier_pymupdf_plan.md`）で取り上げられた指摘のうち「全採用」と記録されているが、本改修で削除する CHUNK_TEXT 関連記述に 210 で残した文言が含まれていないか念のため確認すべき（本レビューでは spec 側を全文確認した結果、削除対象は CHUNK_TEXT 言及のみで 210 由来の必須記述は触っていないと判定）

#### 新規リスク

- **表抽出失敗時の silent skip**: Step 4-B の `try: ... except: pass` で `find_tables()` 例外は無言で吸収される。失敗カウンタや warning ログが無いため、ある銘柄で **常に表抽出が失敗しているのに気付けない**リスクがある。`pages_data[i]['tables']` が空配列（[]）と「`find_tables()` は呼ばれたが 0 個返した」と「例外で潰れた」が区別できない。例外時は `pages_data[i]['tables_error'] = str(e)` のような軽量メタを追加するか、せめてサブプロセス stderr へ件数集計を出すのが安全
- **assistant 解釈の確信度落ち**: text + tables を両方渡すと、assistant が「table の数値を採用すべきか、text の数値を採用すべきか」で迷うケースが発生する可能性（例: tables に「受注高 1,234」、text に「受注高は約 1,200 百万円」と両方ある）。プラン Step 6 §1「表優先」で原則は明示されているが、表と text で**値が不一致**だった場合の優先規約と扱いは明示されていない。「不一致なら表優先、text 値は無視」と 1 行追加すべき
- **「table が無いから text フォールバック」誤判定**: `pages_data[i]['tables']` が空配列の場合、「本当に表が無い（文章のみ）」のか「表があるが PyMuPDF が認識できなかった」のかが区別できず、後者で text に表が文字列として垂れ流された状態を assistant が解釈する経路に入る。062 知見の「PyMuPDF find_tables() は pdfplumber より弱い」前提下では**後者が一定割合発生する**前提でプランを組むべき

### 【プラン MD フォーマット適合性チェック】

- [x] 冒頭に対象ファイルの基準 commit hash あり（`commit 6e762b33`）
- [x] 前提サマリで過去修正と残件が明示されている
- [x] 優先度の定義あり（P0 のみ）。P1/P2 が「なし」で明示されており、優先度インフレ判定は不要
- [x] 各項目が 7 フィールド（症状/該当/根本原因/修正方針/呼び出し側波及/検証/ロールバック）を揃えている
- [x] 修正方針に before/after の両方が記述されている
- [x] 呼び出し側波及が具体ファイル名 + 影響無しの根拠で明示されている
- [△] 「既に〜がある」系の前提と実コードの照合 — **§設計上の重要決定 3 の「月次開示パイプラインで PyMuPDF find_tables() + テキスト併用方式が運用実績あり」が実装と不一致**（実装は pdfplumber 主力、PyMuPDF find_tables() はセクション識別補助のみ）。**最重要チェック項目に違反**
- [x] アンチパターン対応表が末尾にある（該当無しを「—」で明示、これは妥当）
- [△] 検証戦略は smoke / dev / 本番適用判断基準 / 回収手順の 4 段を網羅しているが、**wall time 比較 / pages.json サイズ計測 / 抽出精度のサンプル比較**が含まれておらず、token 削減量のみで判断する設計になっている。本改修の主目的が token 削減であることを考えれば最低限の網羅性はあるが、新規リスク §「PyMuPDF find_tables() の精度不足」が顕在化したときに検出できない設計
- [x] ロールバック手順あり（git revert で 1 コミット復帰可能）
- [x] 優先度インフレなし（P0 はいずれもトークン削減か spec 整合性で根拠あり）
- [x] 関連 commit / 知見 MD / プランへのリンクあり

### 【重大な指摘】（即修正推奨）

#### #1 「月次開示で PyMuPDF find_tables() + テキスト併用方式が運用実績あり」前提の事実誤認

- 箇所: `docs/plans/refactor_orders_soldier_table_extraction_20260519_001056.md:28`、同 `:50`
- 事象: プランは「月次開示パイプラインで PyMuPDF `find_tables()` + テキスト併用方式が既に運用実績あり、仕組みを統一」を本改修の根拠の一つに置いているが、実コードは異なる
- トリガー: 将来このプランを根拠に「PyMuPDF `find_tables()` は十分実績があるから安心」と判断する後段意思決定（例: 他スキル展開、Cloud Run Job 化検討時の精度見積もり）
- 影響: **精度見積もりの根拠が崩れる**。062 知見「PyMuPDF find_tables() は pdfplumber より弱く、複雑表で崩れる」が示すように、複雑表が多い受注高開示で表崩れによる抽出精度低下が起こりうる。プラン採用の根拠の一つを失う
- 根拠: `scripts/extract_monthly_data.py:1717` (`pdfplumber.open(...)`)、`:1719` (`page.extract_tables()`) が表抽出本体。`:1746` `:2156` の `page.find_tables()` (PyMuPDF) は **テーブル直上テキストをセクション識別用に取る bbox 取得**にしか使われていない（L1748-1752）。`docs/knowledges/tools/062_pdf_processing_strategy.md:105` に「PyMuPDF (fitz) ... `find_tables()` は pdfplumber より弱く、複雑表で崩れる」と明記
- 推奨対応 **[検証済み]**:
  - プラン §設計上の重要決定 3 / 前提サマリ §関連 incident の「月次開示で PyMuPDF `find_tables()` 運用実績」記述を、以下のように事実ベースに書き換える:
    - 「月次開示パイプラインの主力表抽出は `pdfplumber.extract_tables()` であり、PyMuPDF `find_tables()` は補助的なセクション識別 bbox 取得のみに使われている（062 知見 §4.1 参照）。本ソルジャーは pdfplumber 不採用で PyMuPDF `find_tables()` を表抽出本体に使う**新規パターン**である。062 知見の通り PyMuPDF の表認識は pdfplumber より弱いため、**表が崩れたときは text フォールバックで救済する判定を Step 6 に明示**する」
  - Step 6 §数値抽出の優先順位 に「**`tables` があっても明らかに崩れている場合（行ヘッダが空・数値カラムが 0・行数が 1）は text 側を採用**」を 1 行追加
  - 採用根拠を変える必要は無いが、「実績ありの安全策」ではなく「PyMuPDF 単独パイプラインを許容する新規パターン」と明記することで将来のリスク監視が機能する

#### #2 `pages_data` サイズ上限ガードの未定義

- 箇所: `docs/plans/refactor_orders_soldier_table_extraction_20260519_001056.md:108-131` (Step 4-B の after コード)、同 `:144-146` (Step 5 表の Read tool スキップ)
- 事象: hit_pages 数 × ページごとの (text + tables) 量に上限が無く、`{ticker}.pages.json` が肥大化した場合に assistant 1 回 Read でも数万 token を消費し、改修目的の「Read tool 直読 85,468 token からの削減」が薄まる可能性
- トリガー: 大型決算短信（30-50 ページ）+ 受注関連記述がページ全体に散在する業種（例: 総合電機・重工系）。プラン §smoke で挙げている 1734・1736・1739 は中小企業中心と推定され、このリスクが smoke で顕在化しない懸念
- 影響: 改修後も 50,000+ token のケースが出る → 削減効果が中央値で 30% 程度に止まる可能性（推定）。本改修の費用対効果評価が間違う
- 根拠: 日本語 1 ページ ≒ 1,500-3,000 文字、JSON 化（`text` + 表の 2 次元配列文字列化）で約 1.3-1.5 倍に膨張。18 ヒットページで楽観 9k token、悲観 50k+ token。プランに上限規定が無い
- 推奨対応 **[方向性]**:
  - Step 4-B に「hit_pages 数が **N=10** を超えた場合は text を 1,500 文字でトリミング、tables のみ全量保持」のような soft cap を追記
  - 検証戦略に「smoke 3 件で `{ticker}.pages.json` のバイト数を ntfy 報告し、最大バイト数を観測値として残す」観点を追加
  - 上限値（N=10 / トリム長 1500）の具体値は smoke 結果から逆算するため初期値は方向性のみ提示

#### #3 `tbl.extract()` 戻り値の正規化指針が無い

- 箇所: `skills/orders_soldier.md:316-324` (Step 6 §数値抽出の優先順位)、`docs/plans/refactor_orders_soldier_table_extraction_20260519_001056.md:148-152` (修正方針 §3)
- 事象: `tbl.extract()` の戻り値は (a) 結合セル `None`、(b) 空セル `""`、(c) 改行入りセル `"行A\n行B"`、(d) 数値の表記揺れ（`1,234` `△100` `-` `―`）を含む。プラン Step 6 と spec Step 6 はいずれも「表があれば行/列の対応が確実」とだけ書いており、上記表現揺れの正規化ルールが空欄
- トリガー: 結合セルを多用する決算短信表（特に「期」「セグメント」「単位」ヘッダの結合セル）。`null` セルが assistant 解釈で「数値 0」と取られて誤抽出
- 影響: トークン削減は達成しても DOCS_WITH_DATA（数値抽出件数）が改修前より落ちる。本改修の本番適用判断基準「`d[]` 要素数の減少が 0 または許容範囲内」を満たせない可能性
- 根拠: PyMuPDF `find_tables()` の API ドキュメント仕様、および 062 知見 §4.1 「複雑表で崩れる」の延長で、結合セル・改行セルが頻発することが推定できる
- 推奨対応 **[方向性]**:
  - Step 6 に「**`tbl.extract()` 戻り値の正規化**」サブセクションを追加し、(a) `None`/`""` は `null` に統一、(b) `"行A\n行B"` は `\n` を ` `（半角空白）に置換、(c) `△100` → `-100`、`―` → `null`、`,` 除去後 int/float キャストを試行、(d) 失敗時は元文字列のまま `r[]` に格納、を明示
  - 数値正規化の具体実装は assistant 推論に委ねず、最低限の規約だけは明文化する

#### #4 表抽出失敗の silent skip（既存実装含む）

- 箇所: `skills/orders_soldier.md:180-184` (Step 4-B の `try: ... except: pass`)
- 事象: `find_tables()` または `tbl.extract()` で例外が出た場合に `except Exception: pass` で握りつぶされ、後段で「表が無かった」と区別できない
- トリガー: 特定 PDF（複雑レイアウト・暗号化・破損）で `find_tables()` 例外
- 影響: ある銘柄で **常に表抽出が失敗しているのに気付けない**。text フォールバックで多少救済されるが、表が必要な数値が常に取りこぼされる
- 根拠: コード L182-184 で `tables=[]` と「例外で潰れた」の区別が消える
- 推奨対応 **[方向性]**:
  - サブプロセス内で `tables_extract_errors: list[str]` を集計し、`pages_data[i]['tables_error_count'] = ...` を埋めるか、サブプロセス stderr へ "table_extract_failed: pdf={path} page={i} err={e}" を 1 行出すかのいずれか
  - assistant 側で `tables_error_count > 0` を見て「表抽出失敗を踏まえて text 偏重で解釈」と判断できるようにする

### 【改善提案】（可読性・保守性）

#### #1 Step 6 §数値抽出の優先順位 §3 「複数表混在時」の判定基準を具体化

- 箇所: `skills/orders_soldier.md:322`
- 現状: 「§抽出対象に該当する表だけを採用」とだけ書かれており、判定方法が assistant 推論に委ねられている
- 提案: 「**各表の見出し行（先頭行）に §抽出対象 キーワード（受注高/受注残/繰越/手持/...）がマッチするものだけ採用**」と判定軸を 1 行明示

#### #2 検証戦略に wall time / pages.json サイズ計測を追加

- 箇所: `docs/plans/refactor_orders_soldier_table_extraction_20260519_001056.md:224-225`
- 現状: 検証は token 削減量のみ
- 提案: smoke test で (a) wall time の改修前後比較（find_tables の追加コストを評価）、(b) `{ticker}.pages.json` のバイト数 max/avg を ntfy 報告、を追加

#### #3 smoke 銘柄に「大型決算短信銘柄」を 1 件追加

- 箇所: `docs/plans/refactor_orders_soldier_table_extraction_20260519_001056.md:224`
- 現状: 1734・1736・1739 のみ
- 提案: 30 ページ超の決算短信を出している総合電機・重工系（例: 6501/6502/7011）から 1 件加え、`pages_data` の上限挙動を観察

#### #4 表と text で値が不一致だった場合の優先規約を Step 6 に追加

- 箇所: `skills/orders_soldier.md:316-324`
- 現状: 「表優先」原則のみで「**不一致時は表優先か text 優先か**」が暗黙
- 提案: 「**tables と text の数値が不一致の場合は tables を採用、text 値は無視**。ただし tables の値が桁ズレ（例: 表 = 123 万円表記、text = 1,230,000 円表記）の場合はラベル単位 `u` フィールドの整合性を再確認」を 1 行追加

#### #5 旧プラン (`210_cr_orders_soldier_pymupdf_plan.md` 由来) との累積影響セクション

- 箇所: プラン MD §関連ドキュメント
- 現状: 210 が「全採用」されたとだけ書かれている
- 提案: 210 で導入された設計上の規約（例: PyMuPDF 一本化 / fallback 経路 / heartbeat タイミング）のうち、本改修で**変更されるもの・維持されるもの**を 1 表にまとめると将来の差分追跡が楽

#### #6 画像 PDF fallback 閾値 500 文字の根拠付記

- 箇所: `docs/plans/refactor_orders_soldier_table_extraction_20260519_001056.md:141`、`skills/orders_soldier.md:202`
- 現状: 「`total_chars < 500`」とだけ書かれ根拠が無い
- 提案: 「決算短信・説明資料は最低でも 5,000 文字以上が標準。500 文字未満は画像 PDF または抽出全失敗とほぼ同義」のような 1 行根拠を付ける

#### #7 中間ファイル パス記法の統一

- 箇所: プラン §修正方針 1) コード、Step 5 表、Step 7 cleanup
- 現状: `/c/tmp/tdnet_orders/{ticker}.pages.json` の単数形と `{ticker}_{idx}.pdf` の連番形式が混在
- 提案: 動作影響は無いが、pages.json は 1 ticker = 1 ファイル、PDF は 1 ticker = N ファイルである旨をプラン Step 4-B 冒頭で 1 行明示しておくと読み手の混乱を防げる

### 【修正例】（必要な箇所のみ）

#### #1 に対する修正案（プラン §設計上の重要決定 3 の書き換え）

```markdown
# before（プラン L50）
3. **既存実装との整合**: 月次開示パイプラインで採用済の PyMuPDF `find_tables()` + テキスト併用方式と仕組みを統一

# after
3. **既存実装との位置付け**: 月次開示パイプライン（`scripts/extract_monthly_data.py`）の主力表抽出は `pdfplumber.extract_tables()` であり、PyMuPDF `find_tables()` は補助的なセクション識別 bbox 取得のみに使われている（062 知見 §4.1 参照）。本ソルジャーは pdfplumber を導入せず PyMuPDF `find_tables()` を表抽出本体に使う**新規パターン**である。062 知見の通り PyMuPDF の表認識は pdfplumber より弱いため、**表が崩れたとき（行ヘッダ空・数値列ゼロ・行数 1 等）は Step 6 §数値抽出の優先順位 §2 に従い text フォールバックで救済**する。
```

#### #3 に対する修正案（spec Step 6 §数値抽出の優先順位）

```markdown
# before（skills/orders_soldier.md L316-324）
1. **`pages_data[i].tables` を優先採用**: 表があれば行/列の対応が確実。各 `tbl.extract()` の戻り値は 2 次元配列で、`h` (列ヘッダ) と `r` (行データ) にそのままマップしやすい
2. **表がない / 表に該当数値が無い場合は `pages_data[i].text` から数値抽出**: 文章記述（例: 「受注残高は前年同期比で大幅に増加」「当四半期の受注高は 1,234 百万円」）を正規表現や文脈解釈で拾う
3. **同一ページに複数の表が混在する場合**: §抽出対象 に該当する表だけを採用（売上 P/L 表・配当方針表等は対象外）

# after
1. **`pages_data[i].tables` を優先採用**: ただし**表が崩れている**（行ヘッダが空 / 数値カラムが 0 / 行数が 1 のみ）場合は §2 にフォールバック。`tbl.extract()` の戻り値正規化ルール:
   - `None` / `""` → `null`
   - `"行A\n行B"`（改行入りセル）→ `\n` を半角空白に置換
   - 数値: `△100` → `-100`、`―` → `null`、`,` 除去後 int/float キャストを試行。失敗時は元文字列のまま `r[]` に格納
   - **tables と text で数値が不一致の場合は tables を採用**（text 値は無視。ただし桁ズレが疑われる場合は `u` フィールドの単位整合性を再確認）
2. **表がない / 表が崩れている / 表に該当数値が無い場合は `pages_data[i].text` から数値抽出**: 文章記述を正規表現や文脈解釈で拾う
3. **同一ページに複数の表が混在する場合**: 各表の見出し行（先頭行）に §抽出対象 キーワード（受注高/受注残/繰越/手持/...）がマッチするものだけ採用
```

### 【確認できなかった事項】

- `pages.json` の**実測サイズ**（smoke test 未実施のため。実装側は commit 5ea7ed39 で完了しているが、Step 7 で都度削除されるため `/c/tmp/tdnet_orders/` 配下に観察可能なサンプルが残っていない）
- 受注高ソルジャーが扱う PDF の `find_tables()` 成功率（062 知見の「弱い」評価は決算短信 BS/PL/CF を対象としたものだが、受注高開示の表形式が同じくらい複雑かは未検証）
- 改修済 spec (`skills/orders_soldier.md` commit 5ea7ed39) が本プランの記述と完全一致しているか — プラン提示の after コード（L100-132）と実装 (L155-192) は一致を確認したが、Step 6 §数値抽出の優先順位 §1 の正規化ルール詳細が実装側に無い（上記改善提案 #1, #4 と修正例 #3 で具体化を提案）
- `data/output/` 配下に過去 run の `pages.json` バックアップ等があるかは未確認（cleanup 後のため）

---

### 不備蓄積ログ追記

004-1 蓄積ログへのエントリ追記は本サブエージェントから実施した（メインエージェント側ではなくレビュアー側が実施）。

---

## レビュー対応記録（提出元） — 2026-05-19 JST

- **対応日**: 2026-05-19
- **対応者**: メインエージェント（Claude Opus 4.7）
- **ユーザー判断**: 重大#1 / #3 / #4 + 改善#1-#7 を**全採用**。重大#2 (pages_data サイズ上限ガード) は**不採用**（データ取りこぼし回避を優先）。
- **対応 commit**: 後述コミット hash 参照（`docs/plans/refactor_orders_soldier_table_extraction_20260519_001056.md` §実装記録 で追跡）

### 採否個別記載

| # | 区分 | 採否 | 反映先 / 備考 |
|---|------|------|--------|
| 重大#1 | 「月次開示で PyMuPDF find_tables 運用実績」事実誤認 → pdfplumber 切替 | **採用** | プラン §設計上の重要決定 §3 を事実ベースに書換。spec Step 4-B を `PyMuPDF text + pdfplumber tables` ハイブリッドに改修。062 知見§4.1 と整合 |
| 重大#2 | `pages_data` サイズ上限ガードの追加 | **不採用** | ユーザー判断: データ取りこぼし回避を優先。設計決定 §11 に不採用理由を明記 |
| 重大#3 | `tbl.extract()` 戻り値正規化規約（None/改行/△） | **採用** | プラン設計決定 §6 + spec Step 4-B サブプロセスに `normalize_table()` 関数を組み込み（結合セル空文字化、改行→空白、△/▲→-） |
| 重大#4 | 表抽出失敗の silent skip 禁止 | **採用** | プラン設計決定 §7 + spec Step 4-B に `table_extract_errors: list[str]` 追加。assistant は ERRORS/HIGHLIGHTS に転記義務（spec §Step 6 §`table_extract_errors` の取扱、§禁止事項、§Step 8 ERRORS 行で明示） |
| 改善#1 | Step 6 §3「複数表混在時」判定基準の具体化 | **採用** | spec §数値抽出の優先順位 §3 で「見出し行に §抽出対象キーワードマッチする表だけ採用」と具体化 |
| 改善#2 | 検証戦略に wall time / pages.json サイズ計測追加 | **採用** | プラン §検証戦略 §1 を 3 種実測値（token + wall time + pages.json バイト数）に強化 |
| 改善#3 | smoke 銘柄に大型決算短信銘柄を追加 | **採用** | プラン §検証戦略 §1 で 6501/6502/7011 から 1 件追加して 4 件構成に |
| 改善#4 | 表と text 値不一致時の優先規約 | **採用** | プラン設計決定 §8 + spec §数値抽出の優先順位 §4 + note 許可キーに `value_conflict_table_priority` 追加 + 禁止事項追加 |
| 改善#5 | 210 由来規約との累積影響表追加 | **採用** | プラン §関連ドキュメント に「210 由来規約と本 v2 改修の累積影響表」追加 |
| 改善#6 | 画像 PDF fallback 閾値 500 文字の根拠付記 | **採用** | プラン設計決定 §9 + spec §Step 5-B 直下に「典型的 1 ページ ≒ 1,500 文字、500 文字未満は画像 PDF 確定」と明記 |
| 改善#7 | 中間ファイル命名規約の明示 | **採用** | プラン設計決定 §10 + spec §Step 5 冒頭に「`_{idx}.pdf` 連番 vs `.pages.json` 単数」明示 |

### 変更ファイル一覧

- `skills/orders_soldier.md` — Step 4-B サブプロセス書換 (PyMuPDF text + pdfplumber tables + normalize_table + table_extract_errors) / Step 5 中間ファイル命名規約 + 閾値根拠 / Step 6 §数値抽出の優先順位を 4 項目に拡張 / note 許可キー追加 / 禁止事項追加 / 注意事項に pdfplumber 依存追記
- `docs/plans/refactor_orders_soldier_table_extraction_20260519_001056.md` — v2 改訂、設計決定 §3 事実訂正、§6-§11 追加、検証戦略強化、累積影響表追加、レビュー対応記録 v2 追加
- `docs/reviews/212_cr_orders_soldier_hybrid_table.md` — 本セクション追記
