# MD AI可読性レビュー: CLAUDE.md再編成プラン（憲法+索引モデル）

- 日時: 2026-05-12 07:34 JST
- 対象: `docs/plans/20260512_072134_claude_md_restructure.md`
- パターン: 1（作成・更新 MD のまっさらレビュー）
- レビュアー: Claude (md-reviewer skill)
- 出力先: `docs/reviews/145_mr_claude_md_restructure_plan.md`

---

## 【サマリー】

レビュー対象の要約: CLAUDE.md（357行）を「原則+禁止事項+導線」に絞った憲法+索引モデルに再編するプラン。Step 0でレビューエージェントの膨張ドライバーを改訂、Step 1で知見MD委譲先を準備、Step 2でCLAUDE.md書き直し（200行以下目標）、Step 3-4で004整理と検証。

- AI可読性評価: **A** -- プランの構造は明確で、AIが各ステップで何をすべきか概ね判断できる。移動元/移動先の対応表は具体的。ただし複数箇所で重要な曖昧さと導線設計の漏れがある。
- 誤読リスク評価: **B** -- 「原則のみ残す」「詳細は委譲」の境界判定基準が不十分なため、実行AIが境界を恣意的に解釈し、防止力のある記述を過剰に削除するリスクがある。
- 主要リスク:
  1. LINE会話モードのコンテキスト圧縮後復旧ルールが068移動で失われるリスク（R-1の対策が不十分）
  2. 「原則か手順か」の判定基準が曖昧で、実行時に過剰削除/削除不足の振れが発生する
  3. Step 0のスキルMD改訂内容がbefore/afterで示されておらず、実行AIの解釈余地が大きい

---

## 【Markdown 品質評価】

### Accuracy / 正確性

- CLAUDE.mdの行数を「357行」としているが、実際のCLAUDE.mdは357行（末尾空行含む）で正確
- md-reviewer.md の行番号参照（L57-62, L315-318, L348-351）は実コードと一致する
- 138_so レビューの RD-1 内容との整合: プランは RD-1 の「パターンDB転換」を入力として取り込んでおり、Step 3 で「P-001テーブルにMR-139追加」を明記している。整合
- code-reviewer.md L348 の参照は正確（§再発防止ルールには記載先を明記する）
- structure-optimizer.md L84-90 の参照は正確（§圧縮による導線喪失リスク付近）

### Completeness / 完全性

- Step 0-4 の全ステップにチェックボックスがあり、完了判定が可能
- 移動元/移動先の対応表が具体的（§LINE会話モード→068、§日付時刻→004等）
- **不足点**: Step 2 の「新構造でCLAUDE.mdを書き直し」が1チェックボックスのみ。357行→200行の書き直しが1ステップでは粒度が粗い。特に §4 基本原則の統合は5つのサブ統合を含む大作業で、中間検証ポイントがない
- **不足点**: Step 1→Step 2 の依存関係が暗黙。Step 1 の知見MD移動が不完全な状態でStep 2 を開始するとデータロストの可能性がある

### Relevance / 関連性

- 背景と目的が明確で、ノイズは少ない
- 「肥大化の構造的原因」分析（md-reviewer スキルMDがドライバー）は独自の洞察で、プランの正当性を裏付ける
- リスクと判断ポイント（R-1〜R-3）は適切に選定されている

### Actionability / 実行可能性

- Step 0 のスキルMD改訂: 変更前/変更後が表形式で示されているが、**実際のafter文面がない**。「知見MD（004等）への追記またはパターンDB（004 §P-001〜）への事例追加を推奨。CLAUDE.mdへの追加は既存原則の改訂が必要な場合のみ」は方向性であって、md-reviewer.md の具体的な修正文案ではない
- Step 2 のCLAUDE.md書き直し: §4の統合設計でMDコードブロック例が提示されているが、§1/§5/§6/§7/§8-11は方針記述のみで完成イメージがない

---

## 【AI 誤読リスク】

### #1 「原則のみ残す」の判定基準が曖昧

- 箇所: `docs/plans/20260512_072134_claude_md_restructure.md:28-29`
- 問題: 「原則 + 禁止事項 + 導線（ポインタ）」に絞ると書かれているが、「何が原則で何が手順か」の判定基準がプラン内で定義されていない
- AIの誤読パターン: 実行AIが「これは手順的だから004に移動」と判断する際、実は「手順に見えるがCLAUDE.mdにないと効かないルール」（例: 監視の4要件、マルチターン待機のデフォルト待機方針）を過剰に移動してしまう
- トリガー: Step 2 の書き直し時に、各ルールを「原則か手順か」で分類する全ての判断場面
- 影響: 防止力のあるルールが知見MDに移動され、コンテキスト圧縮後にロードされず事故が再発する
- 根拠: R-1 で LINE会話モードについてこのリスクが認識されているが、他のルール全体への汎用判定基準として定義されていない

### #2 Step 2の§4.2統合例の「適用例」ポインタの到達可能性

- 箇所: `docs/plans/20260512_072134_claude_md_restructure.md:201-202`
- 問題: §4.2 ユーザー指示優先の末尾に `→ 適用例・判定フロー・事故事例: 004_coding_conventions.md §事故パターンDB P-001` とポインタを置く設計。しかし、004 は高頻度参照テーブルで「スクリプトを新規作成・改修」タスクに紐付いている。ユーザー指示逸脱パターン（P-001）が発火するのは「スクリプト作成」以外の全タスク（手動処理、プラン実行、障害対応等）でも起きる
- AIの誤読パターン: 高頻度参照テーブルのタスク名が「スクリプトを新規作成・改修」であるため、手動処理タスク等では004がロードされず、§4.2のポインタが死文になる
- 根拠: 138_so レビュー L202-203 で同じ問題が指摘済み（「事故パターンDBの参照は『スクリプト作成』とは異なるタスクで発生するため、§指示の字義優先からの直接ポインタが実質的な導線」）。プランの§4.2文案にこの直接ポインタは含まれているが、高頻度参照テーブルへの別名追加は計画されていない
- 影響: 中。§4.2本文にポインタがあるため最低限の導線はあるが、コンテキスト圧縮後に§4.2自体が読まれない場合に004§P-001に到達できない

### #3 MR事故番号除去による参照トレーサビリティの断絶

- 箇所: `docs/plans/20260512_072134_claude_md_restructure.md:294-307`
- 問題: CLAUDE.md内の全MR事故番号（MR-039, MR-071等）を除去する方針だが、知見MD側に全ての事故番号が確実に存在するかの検証ステップがない
- AIの誤読パターン: 実行AIが機械的にMR事故番号を全削除し、一部の事故番号が知見MD側にも存在しない「宙に浮いた事故記録」になる
- トリガー: Step 2 でCLAUDE.mdを書き直す際にMR番号を削除する全ての操作
- 影響: 事故の教訓が完全に失われるケースが発生し得る。特にCLAUDE.mdにしか書かれていない事故番号がある場合
- 根拠: Step 1 の移動先テーブルでは「MR事故番号（全箇所）→ 各知見MD内の該当セクション」「事故記録は知見MD側に既存」と書かれているが、これが**全MR番号について検証済みか**が不明

---

## 【MD 構成リスク】

### #4 Step 0 と Step 2 の責務境界

- 箇所: `docs/plans/20260512_072134_claude_md_restructure.md:81-123` (Step 0) と `docs/plans/20260512_072134_claude_md_restructure.md:146-292` (Step 2)
- 問題: Step 0 は「レビューエージェントのスキルMD改訂」、Step 2 は「CLAUDE.md書き直し」。しかしStep 2 の §1 で「CLAUDE.mdに編集ポリシーを定義」（L150-163）しており、この編集ポリシーの定義（行数上限200行、追加禁止項目等）はStep 0 で定義する「CLAUDE.md追加許可基準」（L109-115）と実質的に同じ内容を異なる粒度で述べている。AIがStep 0の基準とStep 2の§1の基準のどちらを正本とすべきか迷う
- 推奨: Step 0 の「CLAUDE.md追加許可基準」と Step 2 §1 の「編集ポリシー」が同一の制約を指していることを明示し、正本がどちらか（CLAUDE.md §1が正本、スキルMD側はそれを参照）を定義する

### #5 Step 2 の内部構造が不均一

- 箇所: `docs/plans/20260512_072134_claude_md_restructure.md:167-292`
- 問題: §4 基本原則はMDコードブロックで完成イメージが示されている（高い具体性）。一方、§5 長時間運用、§6 実行環境、§7 コーディング規約は箇条書きで「残すもの/削るもの」リストのみ（低い具体性）。§8-11は「現状維持」の一言。実行AIが§4と同じ品質で§5-7を書き直す保証がない
- 推奨: §5-7についても最低限のMDコードブロック例（完成イメージ）を提示するか、「§4の統合例と同様の原則+ポインタ構造で書く」と明示する

---

## 【指示優先順位・文脈境界】

### #6 CLAUDE.md §1 の自己保護ルールとCLAUDE.md §知見管理の関係

- 箇所: `docs/plans/20260512_072134_claude_md_restructure.md:150-163`
- 問題: 再編後のCLAUDE.md §1 に「行数上限200行。超過時は知見MDへの委譲を検討」と書く計画。一方、現行CLAUDE.md §知見管理には「知見ファイル整合義務」「発火タイミング」等があり、これらが200行以内に収まるか検証されていない
- AIの誤読パターン: 200行上限を守るために、知見管理セクションの重要な発火タイミング定義を削除してしまう
- 推奨: 再編後の行数試算（セクション別の概算行数）をプランに含める。「200行以下」が達成可能かの見積もり根拠を示す

### #7 138_so RD-1 のスコープとプランのスコープの関係

- 箇所: `docs/plans/20260512_072134_claude_md_restructure.md:6` (入力: terminal-relay.md #2 (138_so RD-1))
- 問題: 138_so RD-1 の提案は「ルール増殖モデル→パターン認識モデルへのアーキテクチャ転換」。本プランはその一部（CLAUDE.md圧縮）を取り込んでいるが、RD-1 のより広い意図（事故パターンDBの継続的運用設計）はStep 3 の「P-001にMR-139追加」にしか反映されていない。プランがRD-1を完全に消化したのか、一部のみ採用したのかが不明
- 推奨: 138_so RD-1 の各提案項目について、本プランでの取り扱い（採用/部分採用/不採用）を明示する

---

## 【重大な指摘】（即修正）

### #1 LINE会話モードのコンテキスト圧縮後復旧ルールが068移動で到達不能になるリスク

- 箇所: `docs/plans/20260512_072134_claude_md_restructure.md:131-132` (移動先テーブル) および `docs/plans/20260512_072134_claude_md_restructure.md:336-340` (R-1)
- 問題: CLAUDE.md §LINE会話モード（L58-79, 22行）を068に移動する計画。R-1で「CLAUDE.md §5に『圧縮後はmemory→068参照』のトリガー行を残す」と対策を述べている。しかし、コンテキスト圧縮後のAIの行動は「CLAUDE.mdを再読み込み→memoryを読む」であり、CLAUDE.md §5のトリガー行が**圧縮後に残るコンテキストに含まれている保証がない**。現行CLAUDE.md では圧縮後復旧義務がCLAUDE.md本文に直書きされているため効いているが、「068参照」のポインタだけでは068を読むという行動が発火しない可能性がある
- AIの誤読パターン: コンテキスト圧縮後、AIがCLAUDE.md §5を読み「LINE会話モード: send_ntfy_and_wait双方向ループ。→詳細: 068」を見ても、「memory の line_conversation_mode.md を**必ずチェック**」という具体的なトリガー行動が書かれていなければ、memoryチェックを省略する
- トリガー: コンテキスト圧縮が発生した全セッション
- 影響: LINE会話モードが圧縮後に復旧されず、ユーザーがスマホ側で返信を待ち続ける事故。過去にレビュー017-023で6件の事故が発生した同系統の問題
- 根拠: 現行CLAUDE.md L64 に「memory の `line_conversation_mode.md` を必ずチェック」と**直書き**されている。これを068に移動してポインタだけ残すと、ポインタを辿る前にmemoryチェックをスキップする
- 推奨対応: CLAUDE.md §5 の LINE会話モード要約に、最低限「圧縮後は memory line_conversation_mode.md をチェックし、active:true なら 068 §LINE会話モード運用ルール に従い復旧」のトリガー行動を残す。「→詳細: 068」だけでは不十分。[方向性]

### #2 Step 2の粒度不足 — 357行→200行の書き直しが1チェックボックスで定義

- 箇所: `docs/plans/20260512_072134_claude_md_restructure.md:311`
- 問題: Step 2 のチェックボックスは「新構造でCLAUDE.mdを書き直し」「MR事故番号を全て除去」「行数200行以下確認」の3項目のみ。しかしStep 2 本文は§1〜§11の11セクションの再構成を含む大作業であり、中間検証なしに一括で実行すると、特定セクションの過剰削除や導線断絶に気付けない
- AIの誤読パターン: 実行AIが3チェックボックスを「全部まとめてやる」と解釈し、357行を一気に書き換えてgit commitした後に導線断絶が判明する。ロールバックが必要になる
- トリガー: Step 2 実行時
- 影響: 大。CLAUDE.mdはプロジェクト全体の最上位指示ファイルであり、書き直し失敗は全セッションに波及する
- 根拠: CLAUDE.md §破壊的操作「全件一発実行禁止」の原則に照らして、CLAUDE.md自体の全面書き直しも段階的に進めるべき
- 推奨対応: Step 2 を §1-§4 / §5-§7 / §8-§11 / MR事故番号除去 / 行数確認 の5サブステップに分割し、各サブステップ後にgit commitする。[方向性]

### #3 Step 0 のスキルMD改訂が「方向性」のみで具体的修正文案がない

- 箇所: `docs/plans/20260512_072134_claude_md_restructure.md:87-99`
- 問題: Step 0 の変更テーブルは「変更前→変更後」の対応を示しているが、「変更後」欄は方向性の要約であって、md-reviewer.md に書き込む具体的なMarkdown文面ではない。例えば「エスカレーション先を3段階に明確化: (1)知見MD(デフォルト) (2)スキルMD(スキル固有) (3)CLAUDE.md(原則レベルの変更のみ)」は、md-reviewer.md L315-318の具体的な書き換え後テキストではない
- AIの誤読パターン: 実行AIが方向性を解釈して独自の文面を起草する。その文面が意図と異なる場合（例: 「原則レベルの変更のみ」の判定基準が曖昧な文面になる）、再編の根幹が崩れる
- トリガー: Step 0 実行時の md-reviewer.md, code-reviewer.md, structure-optimizer.md の各Edit操作
- 影響: 中。スキルMDの改訂内容がプランの意図と異なると、再膨張防止の効果が不十分になる
- 根拠: CLAUDE.md §ステップ完了検証義務「全手順要素の実施を照合」に照らして、「何を書くか」の具体性が必要
- 推奨対応: Step 0 の各変更箇所について、変更後のMarkdown文面（少なくとも核心部分）をプランに明記する。方向性だけでは実行AIの解釈余地が大きすぎる。[方向性]

---

## 【改善提案】（中優先度）

### #4 「原則か手順か」の判定基準をプランに定義する

- 箇所: `docs/plans/20260512_072134_claude_md_restructure.md:28-29`
- 現状: 「原則 + 禁止事項 + 導線」に絞ると書かれているが、判定基準がない
- 提案: 以下の判定基準をプランに追記する: 「CLAUDE.mdに残す基準: (a) コンテキスト圧縮後に自動ロードされなければ防止力がないもの、(b) 1-3行で表現できるもの、(c) 全タスクに横断的に適用されるもの。3条件のAND。1つでも欠ければ知見MDに委譲」
- 期待効果: 実行AIが各ルールについて機械的に判定でき、過剰削除/削除不足の振れが減る

### #5 MR事故番号除去の事前検証ステップ

- 箇所: `docs/plans/20260512_072134_claude_md_restructure.md:294-307`
- 現状: 「MR事故番号を全て除去」とあるが、知見MD側に全事故番号が存在するかの検証ステップがない
- 提案: Step 2 の前提として「CLAUDE.md内の全MR事故番号をリストアップし、各番号が知見MD側のどのファイル/セクションに存在するかを確認する。知見MD側に存在しない事故番号は、該当する知見MDに先に追記してから除去する」を追加
- 期待効果: 事故記録のデータロスト防止

### #6 再編後の行数試算の追加

- 箇所: `docs/plans/20260512_072134_claude_md_restructure.md:325` (Step 4)
- 現状: 検証ステップに「行数確認（目標200行以下）」はあるが、事前の試算がない
- 提案: 再編後の各セクションの概算行数をプランに含める。例: §1(5行) + §2(10行) + §3(15行) + §4(30行) + §5(15行) + §6(20行) + §7(15行) + §8(15行) + §9(10行) + §10(55行) + §11(5行) = 195行。高頻度参照テーブル（現在約55行）が200行目標の最大変数であることが事前に分かる
- 期待効果: 200行目標が達成可能かの早期判断。高頻度参照テーブルの圧縮要否の判断材料

### #7 設計内容の妥当性レビューへの引き継ぎ提案

- 現状: 本レビュー（md-reviewer）はAI可読性の観点に限定しており、以下の設計判断の妥当性は評価していない:
  - §4.1-4.5 の統合方針が適切か（特に5つの個別ルールを§4.2に統合して防止力が維持されるか）
  - §6 実行環境の「削るもの」リストが妥当か（gcloud Git Bash優先の004委譲で問題ないか等）
  - 200行目標の妥当性自体（短すぎないか）
- 提案: code-reviewer パターン4（計画MDの設計レビュー）への引き継ぎを推奨。特にR-2（個別ルール統合で防止力が下がらないか）は設計判断であり、md-reviewerの責務外

---

## 【ソースコード・仕組み側への波及】

該当なし。本プランはMDファイルの構造変更のみを対象としており、ソースコード変更を含まない。

---

## 【修正文案】

### 文案1: 重大指摘#1 — CLAUDE.md §5 LINE会話モード要約の最低限テンプレート

**before** (プラン L234-237):
```markdown
### LINE会話モード
`send_ntfy_and_wait()` 双方向ループ。send_ntfy一方通行は禁止。
コンテキスト圧縮後は memory `line_conversation_mode.md` をチェックし自発再開。
→ 詳細: `068_line_ntfy_push.md` §LINE会話モード運用ルール
```

**after** (推奨):
```markdown
### LINE会話モード
`send_ntfy_and_wait()` 双方向ループ。send_ntfy一方通行は禁止。
**圧縮後の復旧義務**: memory `line_conversation_mode.md` を**必ずチェック**。`active: true` なら `068_line_ntfy_push.md` §LINE会話モード運用ルールに従いファーストトリガーを自発送信して復旧。
CLI構成は068 §② のテンプレートを参照（記憶ベース構成禁止）。
→ 詳細フロー・例外処理: `068_line_ntfy_push.md` §LINE会話モード運用ルール
```

変更理由: 「memory をチェック」だけでなく「active:true の場合のトリガー行動」と「CLI構成は068参照（記憶禁止）」をCLAUDE.md本文に残すことで、圧縮後の068未読でも最低限の復旧行動が取れる。

---

## 【推奨検証（Step 8）】

### 8a. 正本帰属チェック

- プランで提案されている「CLAUDE.md追加許可基準」（L109-115）の正本はどこか: プラン本文内で定義されているが、実施後はCLAUDE.md §1 と各スキルMDの両方に書かれる。正本はCLAUDE.md §1 とし、スキルMD側は「CLAUDE.md §1の基準に従う」と参照すべき。プランはStep 0で「3スキル共通の基準を各スキルMDに埋め込み」としているが、埋め込み方式だと正本が4箇所に分散する
- 推奨: 基準の正本はCLAUDE.md §1 に置き、スキルMD側は「CLAUDE.md追加の可否判断は CLAUDE.md §1 の編集ポリシーに従う」の1行ポインタとする

### 8b. 上位ルール整合性チェック

- CLAUDE.md §破壊的操作との整合: CLAUDE.mdの全面書き直しは破壊的操作の定義（「間違えた時にアンドゥコストが高い」）に該当する。プランのStep 2は「dry-run→小範囲10件確認→全件展開」に相当する段階的検証が不足している → 重大指摘#2 で指摘済み
- CLAUDE.md §AI手動反復処理の逐次永続化義務: Step 2 でCLAUDE.mdの11セクションを順次書き直す作業は、N>=3の反復処理に該当する。中間コミット規定（10件ごとまたは15分ごと）を適用すべき → 重大指摘#2 の推奨と整合
- CLAUDE.md §新規規約の導入は文書化とアトミック: Step 0 で導入する「CLAUDE.md追加許可基準」は新規規約であり、同じ編集内でルールを明文化する必要がある → プランのStep 0→Step 2 §1 の順序で実施すれば満たされる

### 8c. 副作用シミュレーション

**(i) 単体副作用**: 重大指摘#1の修正文案について -- 修正後のCLAUDE.md §5は4行で「memory必ずチェック」「active:trueなら068参照で復旧」「CLI構成は068参照」を含む。この記述自体がAIに新たな誤読を誘発するリスクは低い。068側に詳細が移動している前提で、CLAUDE.md側は行動トリガーのみ。

**(ii) クロスルール競合**: 再編後のCLAUDE.md §4.2（ユーザー指示優先）と §4.5（マルチターン待機）の間に競合はないか検証。§4.2は「指示を省略/改変するな」、§4.5は「明示的GOがなければ待機」。両者は直交する制約であり競合しない。

**(iii) 状態依存シナリオ**: LINE会話モード active/inactive の各状態で、再編後のCLAUDE.md §5 のルールが適切に適用されるか。active状態: 「memory必ずチェック→active:true→068参照→復旧」。inactive状態: 「memory必ずチェック→active:false→通常モード」。いずれも適切に分岐する。

**(iv) 再発防止策の実効性**: Step 0 の「スキルMD改訂による再膨張防止」は、スキルMDの文面変更という構造的対策。意志依存型ではなく、スキルMD自体が行動指針として機能するため実効性がある。ただし、スキルMDを変更してもAIが「CLAUDE.mdに追加すべき」と判断する余地はゼロにはならない（CLAUDE.md §1 の編集ポリシーで歯止めをかける2段構えが必要）。

### 8d. 事後確認事項

- 再編実施後、最初の3セッションで以下を確認:
  - コンテキスト圧縮後にLINE会話モードの復旧が正常に行われるか
  - 高頻度参照テーブルの全導線が有効か（特に004経由のP-001到達）
  - 再編後のCLAUDE.mdでレビューエージェントが「CLAUDE.mdにルール追加」を推奨しなくなったか
  - 200行以内に収まっているか（セッション開始時の確認）

---

## 【確認できなかった事項】

1. 068_line_ntfy_push.md に「§LINE会話モード運用ルール」セクションが既に存在するかの確認 -- 068のヘッダ部分のみ読み、セクション一覧は未確認。Step 1で新セクションとして追記する計画だが、既存内容との重複がないか要確認
2. 004_coding_conventions.md の現在の行数・構造 -- Step 1で多数のセクションが追記される受け入れ先の容量・構造的余裕は未検証。004自体が肥大化する懸念
3. 高頻度参照テーブルの現在の行数が再編後に200行目標に収まるか -- 現行テーブルは約30行（タスク/ファイルのペア）。これを含めて200行以下が達成可能かは試算なしには判断不能
4. structure-optimizer.md L84-90 の変更が他のスキルとの整合性を崩さないか -- 正確な文面案がないため評価不能
5. プランの設計内容の妥当性（統合方針・削除対象の選定・200行目標の適切性） -- md-reviewerの責務外。code-reviewer パターン4 への引き継ぎを推奨（改善提案#7）
