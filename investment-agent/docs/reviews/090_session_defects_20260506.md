# セッション不備記録: 2026-05-06（月次パイプライン初回RUN検証〜非TDnet整理）

**セッションID**: 2ff782ef-bda2-4fc3-8000-9926875ef204
**作成日**: 2026-05-06
**目的**: セッション中に発見した設定・MD・コード不備を網羅し、該当知見MDに再発防止策を反映する

---

## 不備一覧

### A. アダプタ定義・キー名の不整合（5件）

| # | 不備 | 影響 | 対処状況 | 再発防止先 |
|---|------|------|---------|-----------|
| A1 | `excluded` vs `_excluded` キー名不整合（60 adapter） | コードは `_excluded` のみチェック → 除外が機能しない | **済**: 60件リネーム + コードにフォールバック追加 | 042 MD スキーマ定義に明記済み |
| A2 | `custom_prompt` vs `gemini_custom_prompt` 混在（94件 vs 7件） | Geminiに補足指示が届かない | **済**: コード3箇所にフォールバック追加 | 042 MD スキーマで `custom_prompt` を正式キーに定義済み |
| A3 | extract_adapter.json のスキーマが 042 MD に未定義 | エージェントがキー名を独自決定 → 不整合の温床 | **済**: 042 MD に正式スキーマテーブル追加 | — |
| A4 | 2652 adapter が年次決算書を参照して fields=[] | build_monthly_extractor.py が誤文書をサンプル使用 | **済**: 月次売上報告PDFに基づき手動再構築 | **未**: build_monthly_extractor.py にサンプル選択ロジックの改善が必要 |
| A5 | 7616 adapter のローカルパスが `C:\tmp\` に保存されたまま meta/monthly/ に未反映 | エージェントが正式パスに保存しなかった | **済**: 正式パスに移動+GCS同期 | monthly-error-autofix skill に正式パスの明示追加済み |

### B. コードの欠陥（5件）

| # | 不備 | 影響 | 対処状況 | 再発防止先 |
|---|------|------|---------|-----------|
| B1 | GCS HTML フィルタ欠如（ローカルにはあり、GCS側になし） | 1911 で非月次HTML 37件を全処理 → タイムアウト・無駄なGemini呼び出し | **済**: GCSモードにも月次キーワードフィルタ追加 | **要**: 042 MD に「ローカル/GCSで挙動が異なる箇所」注意書き |
| B2 | Gemini HTML タイムアウト 90秒不足 | 3148 で 104秒の正常レスポンスがタイムアウト | **済**: 90→180秒に延長 | — |
| B3 | excel_gemini GCS パスで API key 未設定時も blob DL | 無駄なダウンロード | **済**: ガード条件追加 | — |
| B4 | `_build_excel_gemini_prompt` にガードレール欠如 | 既存の `_build_extract_prompt` にある禁止事項7項目が未反映 → Gemini誤抽出リスク | **済**: 6項目のガードレール追加 | **要**: 新規プロンプト関数作成時のチェックリスト |
| B5 | `_batch_meta` に `since` キー欠落（excel_gemini 2箇所） | 下流処理でフォールバックに依存 | **済**: 2箇所に明示追加 | — |

### C. 運用・ドキュメント不備（4件）

| # | 不備 | 影響 | 対処状況 | 再発防止先 |
|---|------|------|---------|-----------|
| C1 | `--all` フラグ未使用で全社 extract 未実行（sample=30 デフォルト） | 206社/451社が未処理（46%） | **済**: 042 MD に本運用コマンド（`--all`必須）を明記 | 042 MD §本運用チェックリスト |
| C2 | `--since` が西暦か年度か曖昧 | 誤解による年度ずれリスク | **済**: 042 MD + スクリプト --help に「西暦年（年度ではない）」明記 | — |
| C3 | monthly_adapter_index.csv BOM エンコーディング | `encoding='utf-8'` で読むと KeyError | **済**: `utf-8-sig` に修正 | **要**: 042 MD にCSVエンコーディング注記 |
| C4 | GCS と ローカル adapter の非同期（51件未アップロード） | GCS上は旧 `excluded` キーのまま | **処理中**: BGエージェントで一括同期 | feedback memory「アダプター修正後GCS同期必須」は既存 |

### D. エージェント・スキル不備（1件）

| # | 不備 | 影響 | 対処状況 | 再発防止先 |
|---|------|------|---------|-----------|
| D1 | monthly-error-autofix エージェントが Python venv 探索で迷走 | トークン浪費・作業遅延 | **済**: skill に「実行環境（迷走防止）」セクション追加 | monthly-error-autofix.md |

---

## 知見MD反映チェックリスト

| 対象MD | 反映内容 | ステータス |
|--------|---------|-----------|
| 042_monthly_disclosure_master.md | スキーマ正式定義、_excluded注記、--all必須、--since西暦明記 | **済** |
| 042-1_monthly_error_fix_patterns.md | A4（誤サンプル選択）をパターンDBに追加 | **未** |
| 004_coding_conventions.md | B1（ローカル/GCS挙動差異チェック）、B4（プロンプト関数ガードレール）、C3（CSV BOM） | **未** |
| monthly-error-autofix.md | D1（実行環境セクション） | **済** |

---

## 残作業（VM引き継ぎ対象）

1. 042-1 パターンDB に A4（build_monthly_extractor.py 誤サンプル選択）追記
2. 004 コーディング規約に B1/B4/C3 の再発防止ルール追記
3. 042 MD に C3（CSV BOM注記）追記
