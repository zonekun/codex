FROM python:3.12-slim

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends git \
    && rm -rf /var/lib/apt/lists/*

# AWS Documentation MCP サーバーを GitHub から直接インストール
RUN pip install --no-cache-dir \
    "awslabs.aws-documentation-mcp-server @ git+https://github.com/awslabs/mcp.git#subdirectory=src/aws-documentation-mcp-server"

# uvicorn: SSE ASGI サーバー起動に必要
RUN pip install --no-cache-dir uvicorn

# Cloud Run 用エントリーポイントをコピー
COPY scripts/aws_documentation_mcp_server.py scripts/aws_documentation_mcp_server.py

ENV PYTHONUNBUFFERED=1

EXPOSE 8080

ENTRYPOINT ["python", "scripts/aws_documentation_mcp_server.py"]
